---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: agentruntimes.omnia.altairalabs.ai
spec:
  group: omnia.altairalabs.ai
  names:
    kind: AgentRuntime
    listKind: AgentRuntimeList
    plural: agentruntimes
    shortNames:
    - agent
    - ar
    singular: agentruntime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.replicas.ready
      name: Ready
      type: string
    - jsonPath: .status.activeVersion
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AgentRuntime is the Schema for the agentruntimes API.
          It defines a deployment of a PromptKit agent with its associated configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AgentRuntime
            properties:
              console:
                description: |-
                  console configures the dashboard console UI settings.
                  Use this to customize allowed file attachment types and size limits.
                properties:
                  allowedAttachmentTypes:
                    description: |-
                      allowedAttachmentTypes specifies MIME types allowed for file uploads.
                      Supports specific types ("image/png") and wildcards ("image/*").
                      If not specified, defaults to common types: image/*, audio/*, application/pdf, text/plain, text/markdown.
                    items:
                      type: string
                    type: array
                  allowedExtensions:
                    description: |-
                      allowedExtensions specifies file extensions as fallback for browsers with generic MIME types.
                      If not specified, extensions are inferred from allowedAttachmentTypes.
                    items:
                      type: string
                    type: array
                  maxFileSize:
                    default: 10485760
                    description: maxFileSize is the maximum file size in bytes for
                      attachments.
                    format: int64
                    minimum: 1
                    type: integer
                  maxFiles:
                    default: 5
                    description: maxFiles is the maximum number of files that can
                      be attached to a single message.
                    format: int32
                    maximum: 20
                    minimum: 1
                    type: integer
                  mediaRequirements:
                    description: |-
                      mediaRequirements defines provider-specific requirements for different media types.
                      When not specified, the dashboard applies sensible defaults based on the provider type.
                    properties:
                      audio:
                        description: audio defines requirements for audio files.
                        properties:
                          channels:
                            description: channels is the audio channel count the client
                              should capture/play (1=mono).
                            format: int32
                            type: integer
                          chunkDurationMs:
                            description: |-
                              chunkDurationMs is the audio chunk duration (milliseconds) the runtime
                              batches to the realtime provider for spec.duplex sessions. Smaller values
                              reduce interruption/response latency at the cost of more provider messages;
                              larger values do the reverse. Defaults to 100ms when unset. Only meaningful
                              for spec.duplex.audio (ignored for console file-upload audio).
                            format: int32
                            minimum: 1
                            type: integer
                          format:
                            description: format is the PCM sample format the client
                              should send, e.g. "pcm16".
                            type: string
                          maxDurationSeconds:
                            description: maxDurationSeconds is the maximum audio duration.
                            format: int32
                            minimum: 1
                            type: integer
                          recommendedSampleRate:
                            description: recommendedSampleRate is the optimal sample
                              rate in Hz.
                            format: int32
                            minimum: 1
                            type: integer
                          supportsSegmentSelection:
                            description: supportsSegmentSelection indicates if the
                              provider supports selecting audio segments.
                            type: boolean
                        type: object
                      document:
                        description: document defines requirements for document files
                          (PDFs, etc.).
                        properties:
                          maxPages:
                            description: maxPages is the maximum number of pages that
                              can be processed.
                            format: int32
                            minimum: 1
                            type: integer
                          supportsOCR:
                            description: supportsOCR indicates if the provider supports
                              OCR for scanned documents.
                            type: boolean
                        type: object
                      image:
                        description: image defines requirements for image files.
                        properties:
                          compressionGuidance:
                            description: compressionGuidance provides guidance on
                              image compression.
                            enum:
                            - none
                            - lossless
                            - lossy-high
                            - lossy-medium
                            - lossy-low
                            type: string
                          maxDimensions:
                            description: |-
                              maxDimensions specifies the maximum width and height.
                              Images exceeding these will need to be resized.
                            properties:
                              height:
                                description: height in pixels.
                                format: int32
                                minimum: 1
                                type: integer
                              width:
                                description: width in pixels.
                                format: int32
                                minimum: 1
                                type: integer
                            required:
                            - height
                            - width
                            type: object
                          maxSizeBytes:
                            description: maxSizeBytes is the maximum file size in
                              bytes for images.
                            format: int64
                            minimum: 1
                            type: integer
                          preferredFormat:
                            description: preferredFormat is the format that yields
                              best results with this provider.
                            type: string
                          recommendedDimensions:
                            description: recommendedDimensions specifies optimal dimensions
                              for best results.
                            properties:
                              height:
                                description: height in pixels.
                                format: int32
                                minimum: 1
                                type: integer
                              width:
                                description: width in pixels.
                                format: int32
                                minimum: 1
                                type: integer
                            required:
                            - height
                            - width
                            type: object
                          supportedFormats:
                            description: supportedFormats lists supported image formats
                              (e.g., "png", "jpeg", "gif", "webp").
                            items:
                              type: string
                            type: array
                        type: object
                      video:
                        description: video defines requirements for video files.
                        properties:
                          frameExtractionInterval:
                            description: |-
                              frameExtractionInterval is the interval in seconds between extracted frames.
                              Only applicable when processingMode includes "frames".
                            format: int32
                            minimum: 1
                            type: integer
                          maxDurationSeconds:
                            description: maxDurationSeconds is the maximum video duration.
                            format: int32
                            minimum: 1
                            type: integer
                          processingMode:
                            description: processingMode indicates how video is processed.
                            enum:
                            - frames
                            - transcription
                            - both
                            - native
                            type: string
                          supportsSegmentSelection:
                            description: supportsSegmentSelection indicates if the
                              provider supports selecting video segments.
                            type: boolean
                        type: object
                    type: object
                type: object
              context:
                description: context configures the runtime context store.
                properties:
                  storeRef:
                    description: |-
                      storeRef references a secret containing connection details for the context store.
                      Required for the redis store type (the secret must hold a "url" key).
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  ttl:
                    default: 24h
                    description: ttl is the time-to-live for context entries in duration
                      format (e.g., "24h", "30m").
                    type: string
                  type:
                    default: memory
                    description: type specifies the context store backend.
                    enum:
                    - memory
                    - redis
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: spec.context.storeRef is required when context.type is
                    'redis'
                  rule: self.type == 'memory' || has(self.storeRef)
              duplex:
                description: duplex configures the realtime voice/duplex console for
                  this agent.
                properties:
                  audio:
                    description: |-
                      audio declares the realtime audio format the runtime requires for this
                      agent's duplex sessions (recommendedSampleRate / channels / format). The
                      runtime advertises it as a bounded counter-offer in RuntimeHello; the
                      facade relays it to the client, which captures at that format, or the
                      session fails at open when the client cannot satisfy it. Unset means the
                      runtime accepts the client's proposed format.
                    properties:
                      channels:
                        description: channels is the audio channel count the client
                          should capture/play (1=mono).
                        format: int32
                        type: integer
                      chunkDurationMs:
                        description: |-
                          chunkDurationMs is the audio chunk duration (milliseconds) the runtime
                          batches to the realtime provider for spec.duplex sessions. Smaller values
                          reduce interruption/response latency at the cost of more provider messages;
                          larger values do the reverse. Defaults to 100ms when unset. Only meaningful
                          for spec.duplex.audio (ignored for console file-upload audio).
                        format: int32
                        minimum: 1
                        type: integer
                      format:
                        description: format is the PCM sample format the client should
                          send, e.g. "pcm16".
                        type: string
                      maxDurationSeconds:
                        description: maxDurationSeconds is the maximum audio duration.
                        format: int32
                        minimum: 1
                        type: integer
                      recommendedSampleRate:
                        description: recommendedSampleRate is the optimal sample rate
                          in Hz.
                        format: int32
                        minimum: 1
                        type: integer
                      supportsSegmentSelection:
                        description: supportsSegmentSelection indicates if the provider
                          supports selecting audio segments.
                        type: boolean
                    type: object
                  enabled:
                    description: enabled turns on the realtime voice console for this
                      agent.
                    type: boolean
                  mode:
                    default: audio
                    description: |-
                      mode is the duplex modality. "audio" (default) streams voice only;
                      "audiovideo" additionally streams the browser camera (not yet implemented).
                    enum:
                    - audio
                    - audiovideo
                    type: string
                type: object
              evals:
                description: evals configures realtime eval execution for this agent's
                  sessions.
                properties:
                  enabled:
                    description: enabled activates eval execution for this agent's
                      sessions.
                    type: boolean
                  inline:
                    description: |-
                      inline configures the evals that run synchronously inside the runtime
                      (Pattern C). Lightweight evals (contains, regex, deterministic
                      scorers) belong here. Defaults to groups=["fast-running"] when unset —
                      disjoint from the worker default so no eval runs on both paths.
                    properties:
                      groups:
                        description: |-
                          groups is the set of eval group names this path executes. An
                          absent or empty list uses the built-in default for the path
                          (see EvalConfig.Inline and EvalConfig.Worker).
                        items:
                          type: string
                        type: array
                    type: object
                  podOverrides:
                    description: |-
                      podOverrides customizes the namespace-level eval-worker Pod. Last
                      AgentRuntime to reconcile wins (eval-worker is per-namespace, not
                      per-CRD).
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations are merged into pod annotations. User values override
                          operator-set defaults on key collision.
                        type: object
                      extraEnv:
                        description: extraEnv is appended to every non-operator-injected
                          container's env.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      extraEnvFrom:
                        description: extraEnvFrom is appended to every non-operator-injected
                          container's envFrom.
                        items:
                          description: EnvFromSource represents the source of a set
                            of ConfigMaps or Secrets
                          properties:
                            configMapRef:
                              description: The ConfigMap to select from
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap must
                                    be defined
                                  type: boolean
                              type: object
                              x-kubernetes-map-type: atomic
                            prefix:
                              description: |-
                                Optional text to prepend to the name of each environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            secretRef:
                              description: The Secret to select from
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret must be
                                    defined
                                  type: boolean
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                        type: array
                      extraVolumeMounts:
                        description: |-
                          extraVolumeMounts are appended to every non-operator-injected container's
                          volumeMounts. Schemaless (see extraVolumes).
                        x-kubernetes-preserve-unknown-fields: true
                      extraVolumes:
                        description: |-
                          extraVolumes are appended to the PodSpec.Volumes. Schemaless to avoid
                          inlining the full corev1.Volume union (~30 volume types) into the CRD;
                          validated by the apiserver at Deployment apply.
                        x-kubernetes-preserve-unknown-fields: true
                      imagePullSecrets:
                        description: imagePullSecrets are appended to the pod's imagePullSecrets.
                        items:
                          description: |-
                            LocalObjectReference contains enough information to let you locate the
                            referenced object inside the same namespace.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels are merged into pod labels. Operator-set labels take precedence
                          on key collision (they are load-bearing for Service selectors).
                        type: object
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: nodeSelector keys are merged with operator-set
                          keys; user values win on collision.
                        type: object
                      priorityClassName:
                        description: priorityClassName overrides the operator-default
                          priority class when set.
                        type: string
                      serviceAccountName:
                        description: serviceAccountName overrides the default service
                          account.
                        type: string
                      tolerations:
                        description: tolerations are appended to the pod's tolerations.
                        items:
                          description: |-
                            The pod this Toleration is attached to tolerates any taint that matches
                            the triple <key,value,effect> using the matching operator <operator>.
                          properties:
                            effect:
                              description: |-
                                Effect indicates the taint effect to match. Empty means match all taint effects.
                                When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                              type: string
                            key:
                              description: |-
                                Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                              type: string
                            operator:
                              description: |-
                                Operator represents a key's relationship to the value.
                                Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                Exists is equivalent to wildcard for value, so that a pod can
                                tolerate all taints of a particular category.
                                Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                              type: string
                            tolerationSeconds:
                              description: |-
                                TolerationSeconds represents the period of time the toleration (which must be
                                of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                it is not set, which means tolerate the taint forever (do not evict). Zero and
                                negative values will be treated as 0 (evict immediately) by the system.
                              format: int64
                              type: integer
                            value:
                              description: |-
                                Value is the taint value the toleration matches to.
                                If the operator is Exists, the value should be empty, otherwise just a regular string.
                              type: string
                          type: object
                        type: array
                    type: object
                  rateLimit:
                    description: rateLimit configures eval execution rate limits.
                    properties:
                      maxConcurrentJudgeCalls:
                        default: 5
                        description: maxConcurrentJudgeCalls is the maximum concurrent
                          LLM judge API calls.
                        format: int32
                        minimum: 1
                        type: integer
                      maxEvalsPerSecond:
                        default: 50
                        description: maxEvalsPerSecond is the maximum number of evals
                          to execute per second.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  sampling:
                    description: sampling configures eval sampling rates to control
                      cost.
                    properties:
                      defaultRate:
                        default: 100
                        description: defaultRate is the default sampling percentage
                          (0-100) for all evals.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      extendedRate:
                        default: 10
                        description: |-
                          extendedRate is the sampling percentage (0-100) for extended evals
                          (model-powered evaluations that call an external service).
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                    type: object
                  sessionCompletion:
                    description: |-
                      sessionCompletion configures how session completion is detected
                      for on_session_complete evals.
                    properties:
                      inactivityTimeout:
                        default: 5m
                        description: |-
                          inactivityTimeout is the duration after the last message before a session
                          is considered complete. Uses Go duration format (e.g., "5m", "1h").
                        type: string
                    type: object
                  worker:
                    description: |-
                      worker configures the evals that run out-of-band in the eval-worker
                      (Pattern A). LLM-judge evals and other long-running or external
                      checks belong here. Defaults to groups=["long-running","external"]
                      when unset.
                    properties:
                      groups:
                        description: |-
                          groups is the set of eval group names this path executes. An
                          absent or empty list uses the built-in default for the path
                          (see EvalConfig.Inline and EvalConfig.Worker).
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              externalAuth:
                description: |-
                  externalAuth configures authentication for data-plane traffic to
                  this agent's facades (external apps streaming via WebSocket or A2A).
                  When unset, the agent is reachable only from the management plane
                  (the dashboard's debug view) — no customer traffic until at least
                  one validator is filled in. Applied to each facade's external auth
                  chain.
                properties:
                  clientKeys:
                    description: |-
                      clientKeys configures per-caller client keys for this agent. Each
                      key is stored as a Kubernetes Secret in the agent's namespace with
                      a sha256 hash of the raw value, claims, and expiry. Created via the
                      dashboard UI; never stored in the CR. The presence of this field
                      (even an empty struct) tells the facade to treat keys labelled for
                      this agent as valid.
                    properties:
                      defaultRole:
                        default: viewer
                        description: |-
                          defaultRole is applied to client keys that don't specify their own
                          claims. Roles are arbitrary claims, not a fixed set — any string is
                          valid here.
                        type: string
                      trustEndUserHeader:
                        description: |-
                          trustEndUserHeader lets the caller forward the end-user identity
                          via the X-End-User-Id request header. Off by default — when off,
                          Identity.EndUser is set equal to Identity.Subject (the client key's
                          own identity), so per-user audit granularity is coarse.

                          Turn on only when the calling app is trusted to faithfully forward
                          user context. A malicious app holding a valid key can spoof
                          arbitrary end-users, so ToolPolicy rules gating on identity.endUser
                          must be paired with an app-level trust assessment.
                        type: boolean
                    type: object
                  edgeTrust:
                    description: |-
                      edgeTrust consumes claim-headers injected by an upstream JWT
                      validator (Istio RequestAuthentication + outputClaimToHeaders, an
                      API gateway, or any other edge that terminates the JWT). The
                      facade does NOT re-verify the token — it trusts the configured
                      headers and maps them onto the Identity struct + the downstream
                      X-Omnia-Claim-<name> contract.

                      Operator MUST ensure the configured claim-headers cannot be
                      injected by anyone other than the trusted edge (Istio's
                      AuthorizationPolicy already strips inbound claim-headers on the
                      chart's authentication.enabled=true setup).
                    properties:
                      claimsFromHeaders:
                        additionalProperties:
                          type: string
                        description: |-
                          claimsFromHeaders lists any additional inbound headers whose
                          values should be exposed to ToolPolicy as X-Omnia-Claim-<claim>.
                          Keyed by the inbound header name (case-insensitive); value is the
                          claim name to emit.

                          Example: {"x-user-groups": "groups"} makes Istio-injected group
                          claims visible to CEL rules as identity.claims.groups.
                        type: object
                      headerMapping:
                        description: |-
                          headerMapping maps outbound X-Omnia-Claim-<name> keys to the
                          inbound headers the upstream edge is known to inject.

                          The defaults cover the chart's current authentication.enabled=true
                          layout (charts/omnia/templates/gateway/authentication.yaml):
                            subject: "x-user-id"
                            endUser: "x-user-id"
                            email:   "x-user-email"

                          The role header (default "x-user-roles") is always read into
                          identity.claims.role; it is not configurable here. Additional
                          headers can be surfaced as claims via claimsFromHeaders.

                          Operators running a different edge (custom EnvoyFilter, API
                          gateway, service mesh other than Istio) override these to name the
                          headers their edge actually emits.
                        properties:
                          email:
                            type: string
                          endUser:
                            type: string
                          subject:
                            type: string
                        type: object
                    type: object
                  oidc:
                    description: |-
                      oidc validates JWTs issued by a customer's OIDC provider inside
                      the facade. Uses the standard discovery document at
                      {issuer}/.well-known/openid-configuration to fetch JWKS. Caches
                      keys in a Secret alongside the AgentRuntime.
                    properties:
                      audience:
                        description: audience is the expected `aud` claim value.
                        minLength: 1
                        type: string
                      claimMapping:
                        description: |-
                          claimMapping maps JWT claim names to the internal identity fields.
                          Defaults: sub→Subject, sub→EndUser. The IdP's role/group claims
                          pass through to identity.claims.<name>.
                        properties:
                          endUser:
                            description: |-
                              endUser names the claim used to identify the end-user on whose
                              behalf this token was issued. Defaults to "sub" — correct for
                              end-user tokens. For service/client-credentials tokens carrying
                              an actor or on-behalf-of claim, set this to that claim name (e.g.,
                              "actor", "on_behalf_of") so the end-user is extracted rather than
                              the calling service.

                              If the named claim is missing from a given token, the validator
                              falls back to Subject.
                            type: string
                          subject:
                            description: |-
                              subject names the claim used as the caller's stable identifier
                              (the token-holder — app or user). Defaults to "sub".
                            type: string
                        type: object
                      issuer:
                        description: |-
                          issuer is the OIDC issuer URL (without trailing slash). Controller
                          fetches {issuer}/.well-known/openid-configuration.
                        minLength: 1
                        type: string
                    required:
                    - audience
                    - issuer
                    type: object
                type: object
              extraPodAnnotations:
                additionalProperties:
                  type: string
                description: |-
                  extraPodAnnotations defines additional annotations to add to the agent pods.
                  Use this for integrations like service meshes, logging agents, or monitoring tools.
                type: object
              facades:
                description: |-
                  facades composes one or more single-protocol facades on top of the
                  shared agent substrate. Each entry is one protocol surface (websocket,
                  a2a, rest, or mcp); all run co-resident in the same pod. Agent mode uses
                  websocket and/or a2a; function mode uses rest (required) and optionally
                  mcp. Must be non-empty with no duplicate types (CEL-validated).
                items:
                  description: FacadeConfig defines the configuration for the client-facing
                    facade.
                  properties:
                    a2a:
                      description: |-
                        a2a configures the A2A protocol surface. Only meaningful on a
                        type=a2a facade. When the agent also has a websocket facade, A2A is a
                        secondary listener on a2a.port (default 9999); when a2a is the only
                        agent-mode facade it is the primary listener (default 8080). Carries
                        the A2A TTLs, task store, agent-card, and outbound clients.
                      properties:
                        agentCard:
                          description: agentCard configures the Agent Card served
                            at /.well-known/agent.json.
                          properties:
                            capabilities:
                              description: capabilities describes protocol features
                                the agent supports.
                              properties:
                                pushNotifications:
                                  description: pushNotifications indicates whether
                                    the agent supports push notifications.
                                  type: boolean
                                streaming:
                                  description: streaming indicates whether the agent
                                    supports streaming responses via SSE.
                                  type: boolean
                              type: object
                            defaultInputModes:
                              description: defaultInputModes lists supported input
                                content types (e.g., "text", "audio").
                              items:
                                type: string
                              type: array
                            defaultOutputModes:
                              description: defaultOutputModes lists supported output
                                content types (e.g., "text", "audio").
                              items:
                                type: string
                              type: array
                            description:
                              description: description is a human-readable description
                                of the agent.
                              type: string
                            name:
                              description: name is the agent's display name.
                              minLength: 1
                              type: string
                            organization:
                              description: organization is the name of the organization
                                that provides this agent.
                              type: string
                            skills:
                              description: skills lists the agent's capabilities for
                                discovery.
                              items:
                                description: AgentSkillSpec describes a specific skill
                                  an agent can perform.
                                properties:
                                  description:
                                    description: description explains what this skill
                                      does.
                                    type: string
                                  examples:
                                    description: examples provides example prompts
                                      for this skill.
                                    items:
                                      type: string
                                    type: array
                                  id:
                                    description: id is the unique identifier for this
                                      skill.
                                    minLength: 1
                                    type: string
                                  name:
                                    description: name is the human-readable name for
                                      this skill.
                                    minLength: 1
                                    type: string
                                  tags:
                                    description: tags are keywords for categorization
                                      and search.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - id
                                - name
                                type: object
                              type: array
                            version:
                              description: version is the agent's version string.
                              type: string
                          required:
                          - name
                          type: object
                        clients:
                          description: |-
                            clients configures connections to other A2A agents.
                            Each client can reference an in-cluster AgentRuntime or an external URL.
                          items:
                            description: A2AClientSpec configures an A2A client connection
                              to another agent.
                            properties:
                              agentRuntimeRef:
                                description: |-
                                  agentRuntimeRef references another AgentRuntime in the cluster.
                                  The controller resolves this to a service URL using the target's status.
                                  Mutually exclusive with url.
                                properties:
                                  name:
                                    description: name is the AgentRuntime resource
                                      name.
                                    minLength: 1
                                    type: string
                                  namespace:
                                    description: |-
                                      namespace is the namespace of the target AgentRuntime.
                                      Defaults to the same namespace as the referencing AgentRuntime.
                                    type: string
                                required:
                                - name
                                type: object
                              authentication:
                                description: authentication configures credentials
                                  for outgoing calls to this agent.
                                properties:
                                  secretRef:
                                    description: 'secretRef references a Secret containing
                                      a bearer token (key: "token").'
                                    properties:
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                              exposeAsTools:
                                description: |-
                                  exposeAsTools registers the remote agent's skills as local tools
                                  via PromptKit's A2A Tool Bridge.
                                type: boolean
                              name:
                                description: name is a unique identifier for this
                                  client within the agent.
                                minLength: 1
                                type: string
                              url:
                                description: |-
                                  url is the direct URL of an external A2A agent endpoint.
                                  Used instead of agentRuntimeRef for agents outside the cluster.
                                  Mutually exclusive with agentRuntimeRef.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                        conversationTTL:
                          default: 30m
                          description: |-
                            conversationTTL is how long idle conversations are retained before eviction.
                            Uses Go duration format (e.g., "30m", "1h"). Defaults to "30m".
                          type: string
                        enabled:
                          description: |-
                            enabled adds A2A as an additional endpoint alongside the primary facade.
                            Only meaningful when facade.type is NOT "a2a" (i.e., websocket or grpc).
                            When facade.type is "a2a", A2A is always the primary protocol regardless of this field.
                          type: boolean
                        port:
                          default: 9999
                          description: |-
                            port is the TCP port for the A2A endpoint in dual-protocol mode.
                            Defaults to 9999. Only used when enabled is true and facade.type is not "a2a".
                          format: int32
                          type: integer
                        taskStore:
                          description: |-
                            taskStore configures the task persistence backend.
                            Defaults to in-memory. Set type to "redis" for persistence across restarts.
                          properties:
                            redisSecretRef:
                              description: |-
                                redisSecretRef references a Secret containing a Redis connection URL
                                in a key named "url". Takes precedence over redisURL.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            redisURL:
                              description: |-
                                redisURL is the Redis connection URL when type is "redis".
                                Format: redis://[:password@]host:port[/db]
                              type: string
                            type:
                              allOf:
                              - enum:
                                - memory
                                - redis
                              - enum:
                                - memory
                                - redis
                              default: memory
                              description: type is the task store backend type. Defaults
                                to "memory".
                              type: string
                          type: object
                        taskTTL:
                          default: 1h
                          description: |-
                            taskTTL is how long completed/failed/canceled tasks are retained before eviction.
                            Uses Go duration format (e.g., "1h", "30m"). Defaults to "1h".
                          type: string
                      type: object
                    clientToolTimeout:
                      description: |-
                        clientToolTimeout is the max time to wait for client tool responses per turn.
                        Defaults to 60s.
                      type: string
                    drainTimeout:
                      description: |-
                        drainTimeout is how long the facade keeps serving active realtime calls
                        after receiving SIGTERM (rollout/drain/scale-down) before cleanly tearing
                        down the remainder. Duration format (e.g. "30s", "2m"). New calls stop
                        immediately on drain regardless. Defaults to 30s when unset.
                      type: string
                    expose:
                      description: |-
                        expose opts this agent into operator-provisioned external exposure.
                        Opt-in: an agent is never externally reachable unless this is set AND the
                        platform has a default-exposure Gateway configured (Helm
                        `defaultExposure`). When both hold, the operator creates a host-based
                        HTTPRoute targeting this agent's facade Service, surfaced via
                        status.facade.endpoints (#1553). Exposure does NOT add authentication —
                        spec.externalAuth is still the gate; an exposed agent with no externalAuth
                        validators is management-plane-only at the facade.
                      properties:
                        enabled:
                          description: |-
                            enabled creates an external HTTPRoute for this agent. Requires the platform
                            to have a default-exposure Gateway configured (Helm `defaultExposure`);
                            when no Gateway is configured this is a no-op. Defaults to false.
                          type: boolean
                        host:
                          description: |-
                            host overrides the generated hostname (`{name}.{namespace}.{baseDomain}`).
                            Set for a custom domain on an individual agent. Must be a hostname the
                            configured Gateway's listener accepts (e.g. covered by its TLS cert).
                          type: string
                      type: object
                    extraEnv:
                      description: |-
                        extraEnv defines additional environment variables for the facade container.
                        Use this for debugging (e.g., LOG_LEVEL=debug) or custom configuration.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                    handler:
                      default: runtime
                      description: |-
                        handler specifies the message handler mode.
                        "echo" returns input messages back (for testing connectivity).
                        "demo" provides streaming responses with simulated tool calls (for demos).
                        "runtime" uses the runtime framework in the container (default, for production).
                      enum:
                      - echo
                      - demo
                      - runtime
                      type: string
                    image:
                      description: |-
                        image overrides the default facade container image.
                        Use this to specify a custom facade image or private registry.
                      type: string
                    managementPlane:
                      description: |-
                        managementPlane gates this facade's internal management-plane twin
                        listener. Default true: the operator allocates an internal port,
                        publishes it under status.managementEndpoints, and the facade serves a
                        management-plane-only auth chain there (the dashboard's "Try this agent"
                        and other in-cluster callers dial it). Set false for an external-only
                        facade — no internal listener, no *-mgmt Service port, no status entry;
                        the external listener is unaffected. Replaces the former agent-global
                        externalAuth.allowManagementPlane.
                      type: boolean
                    mcp:
                      description: |-
                        mcp configures the MCP (Model Context Protocol) surface. Only
                        meaningful on a type=mcp facade (function mode). The pod serves
                        Streamable HTTP MCP on mcp.port (default 9998) alongside the function
                        rest facade.
                      properties:
                        enabled:
                          description: enabled turns the MCP server on. Default false.
                          type: boolean
                        port:
                          description: port is the listen port for the MCP server.
                            Default 9998.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                      type: object
                    port:
                      default: 8080
                      description: port is the port number for the facade service.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    type:
                      default: websocket
                      description: type specifies the facade protocol type.
                      enum:
                      - websocket
                      - a2a
                      - rest
                      - mcp
                      - custom
                      type: string
                  required:
                  - type
                  type: object
                maxItems: 4
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              framework:
                description: |-
                  framework specifies which agent framework to use.
                  Defaults to PromptKit, the only framework this repo builds and tests.
                  Any other type (langchain, custom) requires an explicit image
                  via spec.framework.image or an operator --framework-image entry, and
                  must implement the omnia.runtime.v1 gRPC contract.
                properties:
                  image:
                    description: |-
                      image overrides the default container image for the framework.
                      Required when type is "custom".
                      For built-in frameworks, this allows using a custom build or private registry.
                    type: string
                  type:
                    default: promptkit
                    description: type specifies the agent framework to use.
                    enum:
                    - promptkit
                    - langchain
                    - custom
                    type: string
                  version:
                    description: |-
                      version specifies the framework version to use.
                      If not specified, the latest supported version is used.
                    type: string
                required:
                - type
                type: object
              inputSchema:
                description: |-
                  inputSchema is the JSON Schema that incoming Function payloads are
                  validated against. Required when spec.mode is 'function'; forbidden
                  otherwise (CEL-gated). Stored as a raw JSON object; consumers
                  validate via santhosh-tekuri/jsonschema.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              media:
                description: media configures media file resolution for mock provider
                  responses.
                properties:
                  basePath:
                    default: /etc/omnia/media
                    description: |-
                      basePath is the base directory for resolving mock:// URLs.
                      Defaults to /etc/omnia/media if not specified.
                    type: string
                  storage:
                    description: |-
                      storage configures the media-storage backend the operator provisions
                      into the facade (uploads) and runtime (attachment-reference resolution)
                      containers. When unset, media storage is disabled.
                    properties:
                      azure:
                        description: AzureMediaBackend configures Azure Blob Storage.
                        properties:
                          account:
                            minLength: 1
                            type: string
                          container:
                            minLength: 1
                            type: string
                          prefix:
                            type: string
                        required:
                        - account
                        - container
                        type: object
                      defaultTTL:
                        description: defaultTTL is the retention TTL for stored media
                          (Go duration).
                        type: string
                      downloadURLTTL:
                        description: downloadURLTTL bounds presigned download URLs.
                        type: string
                      gcs:
                        description: GCSMediaBackend configures Google Cloud Storage
                          (workload identity / ADC).
                        properties:
                          bucket:
                            minLength: 1
                            type: string
                          prefix:
                            type: string
                        required:
                        - bucket
                        type: object
                      local:
                        description: LocalMediaBackend stores media on a facade-served
                          local path.
                        properties:
                          basePath:
                            description: |-
                              basePath is the on-disk directory the facade serves media from and the
                              runtime resolves references against. When volumeClaim is set it is the
                              mount path for that PVC.
                            minLength: 1
                            type: string
                          volumeClaim:
                            description: |-
                              volumeClaim names an existing PersistentVolumeClaim to mount at basePath
                              in both the facade and runtime containers. Use a ReadWriteMany PVC so
                              uploads written by the facade are durable and readable by the runtime
                              across pods. When empty, no volume is provisioned — the local backend
                              then requires a writable basePath (the agent containers have a read-only
                              root filesystem), so an explicit volumeClaim (or podOverrides volume) is
                              needed for local storage to function.
                            type: string
                        required:
                        - basePath
                        type: object
                      maxFileSizeBytes:
                        description: maxFileSizeBytes caps a single stored object.
                        format: int64
                        minimum: 1
                        type: integer
                      s3:
                        description: S3MediaBackend configures S3 / S3-compatible
                          (MinIO) storage.
                        properties:
                          bucket:
                            minLength: 1
                            type: string
                          endpoint:
                            description: endpoint, when set (e.g. MinIO), forces path-style
                              addressing.
                            type: string
                          prefix:
                            type: string
                          region:
                            type: string
                        required:
                        - bucket
                        type: object
                      secretRef:
                        description: |-
                          secretRef supplies explicit credentials (S3 AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY,
                          Azure account key). Omit for keyless workload-identity access. GCS uses
                          workload identity only.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      type:
                        default: none
                        description: type selects the storage backend.
                        enum:
                        - none
                        - local
                        - s3
                        - gcs
                        - azure
                        type: string
                      uploadURLTTL:
                        description: uploadURLTTL bounds presigned upload URLs.
                        type: string
                    required:
                    - type
                    type: object
                    x-kubernetes-validations:
                    - message: type s3 requires spec.media.storage.s3
                      rule: self.type != 's3' || has(self.s3)
                    - message: type gcs requires spec.media.storage.gcs
                      rule: self.type != 'gcs' || has(self.gcs)
                    - message: type azure requires spec.media.storage.azure
                      rule: self.type != 'azure' || has(self.azure)
                    - message: type local requires spec.media.storage.local
                      rule: self.type != 'local' || has(self.local)
                type: object
              memory:
                description: memory configures cross-session memory for this agent.
                properties:
                  enabled:
                    description: |-
                      Enabled controls whether cross-session memory is active.
                      Memory is disabled by default.
                    type: boolean
                  retrieval:
                    description: Retrieval configures memory retrieval behavior.
                    properties:
                      accessFilter:
                        description: AccessFilter configures retrieval-time access
                          control (a deny filter).
                        properties:
                          denyCEL:
                            description: |-
                              DenyCEL is a CEL expression over `metadata` (a map<string, dyn> of the
                              retrieved item's metadata). Items for which it evaluates to true are
                              dropped. Empty disables filtering. Example:
                              metadata.url.contains("restricted")
                            type: string
                        type: object
                      enabled:
                        description: |-
                          Enabled controls whether ambient RAG retrieval runs — the per-turn
                          auto-injection of relevant memories into the prompt. Defaults to true
                          (when unset) so memory.enabled keeps its existing behavior. Set to false
                          to keep the memory tools without auto-injecting memories every turn.
                        type: boolean
                      limit:
                        description: Limit is the maximum number of memories injected
                          per turn.
                        format: int32
                        maximum: 50
                        minimum: 1
                        type: integer
                      strategy:
                        description: Strategy selects the retrieval mode.
                        enum:
                        - keyword
                        - semantic
                        - composite
                        type: string
                    type: object
                  tools:
                    description: |-
                      Tools configures the memory tools (memory__remember / memory__recall)
                      exposed to the LLM. Independent of retrieval: an agent can have ambient
                      RAG without the tools, or the tools without RAG.
                    properties:
                      enabled:
                        description: |-
                          Enabled controls whether the memory tools (memory__remember /
                          memory__recall) are active. Defaults to true (when unset) so
                          memory.enabled keeps its existing behavior. Set to false for read-only
                          ambient RAG over a curated store without letting the agent write or
                          explicitly recall.
                        type: boolean
                    type: object
                type: object
              mode:
                default: agent
                description: |-
                  mode controls how the AgentRuntime is invoked. "agent" (default) is
                  the existing conversational runtime (websocket and/or a2a facades);
                  "function" exposes the pack as a one-shot, structured-I/O HTTP endpoint
                  at POST /functions/{name} (a rest facade, optionally with an mcp
                  facade). When set to "function", spec.inputSchema and spec.outputSchema
                  are required.
                enum:
                - agent
                - function
                type: string
              outputFormat:
                description: |-
                  outputFormat controls how the model is asked to format its response in
                  function mode. "text" = free-form (validated post-hoc by the facade),
                  "json" = provider JSON mode (valid JSON, shape unenforced), "json_schema"
                  = provider structured output bound to outputSchema. When unset on a
                  function-mode runtime it defaults to "json_schema". Forbidden when
                  spec.mode is not 'function' (CEL-gated).
                enum:
                - text
                - json
                - json_schema
                type: string
              outputSchema:
                description: |-
                  outputSchema is the JSON Schema that the Function's response is
                  validated against before being returned to the caller. A
                  non-conforming model output is rejected with HTTP 502 and the raw
                  output is returned in the response body for debugging (no
                  in-runtime retry). Required when spec.mode is 'function'; forbidden
                  otherwise (CEL-gated).
                type: object
                x-kubernetes-preserve-unknown-fields: true
              podOverrides:
                description: |-
                  podOverrides lets operators customize the facade+runtime pod's
                  scheduling, service account, labels, annotations, volumes, and env
                  sourced from CSI secret-stores or ConfigMaps.

                  Pod-level fields apply to the pod. Container-level fields
                  (extraEnv, extraEnvFrom, extraVolumeMounts) apply to both the facade
                  and runtime containers but NOT to operator-injected sidecars
                  (e.g. policy-broker). Per-container env overrides remain available
                  via spec.facades[].extraEnv and spec.runtime.extraEnv.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      annotations are merged into pod annotations. User values override
                      operator-set defaults on key collision.
                    type: object
                  extraEnv:
                    description: extraEnv is appended to every non-operator-injected
                      container's env.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  extraEnvFrom:
                    description: extraEnvFrom is appended to every non-operator-injected
                      container's envFrom.
                    items:
                      description: EnvFromSource represents the source of a set of
                        ConfigMaps or Secrets
                      properties:
                        configMapRef:
                          description: The ConfigMap to select from
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                        prefix:
                          description: |-
                            Optional text to prepend to the name of each environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        secretRef:
                          description: The Secret to select from
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    type: array
                  extraVolumeMounts:
                    description: |-
                      extraVolumeMounts are appended to every non-operator-injected container's
                      volumeMounts. Schemaless (see extraVolumes).
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: |-
                      extraVolumes are appended to the PodSpec.Volumes. Schemaless to avoid
                      inlining the full corev1.Volume union (~30 volume types) into the CRD;
                      validated by the apiserver at Deployment apply.
                    x-kubernetes-preserve-unknown-fields: true
                  imagePullSecrets:
                    description: imagePullSecrets are appended to the pod's imagePullSecrets.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      labels are merged into pod labels. Operator-set labels take precedence
                      on key collision (they are load-bearing for Service selectors).
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector keys are merged with operator-set keys;
                      user values win on collision.
                    type: object
                  priorityClassName:
                    description: priorityClassName overrides the operator-default
                      priority class when set.
                    type: string
                  serviceAccountName:
                    description: serviceAccountName overrides the default service
                      account.
                    type: string
                  tolerations:
                    description: tolerations are appended to the pod's tolerations.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              privacyPolicyRef:
                description: |-
                  privacyPolicyRef references a SessionPrivacyPolicy in this agent's namespace
                  that overrides the service group's policy (if any) for this specific agent.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              promptPackRef:
                description: promptPackRef references the PromptPack containing agent
                  prompts and configuration.
                properties:
                  name:
                    description: name is the name of the PromptPack resource.
                    minLength: 1
                    type: string
                  track:
                    description: |-
                      track follows a release channel instead of pinning a version:
                      "stable" selects the highest non-prerelease version; "prerelease" selects
                      the highest version overall. Mutually exclusive with version.
                    enum:
                    - stable
                    - prerelease
                    type: string
                  version:
                    description: version pins an exact PromptPack version. Mutually
                      exclusive with track.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: promptPackRef.version and promptPackRef.track are mutually
                    exclusive
                  rule: '!(has(self.version) && has(self.track))'
              providers:
                description: |-
                  providers is a list of named provider references.
                  Each entry maps a logical name to a Provider CRD.
                  The "default" name is used as the primary provider for the runtime.
                items:
                  description: |-
                    NamedProviderRef associates a name with a Provider CRD reference.
                    The name is used to look up providers by role (e.g. "default", "judge").
                  properties:
                    name:
                      description: name is the logical name for this provider (e.g.
                        "default", "judge", "embeddings").
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    providerRef:
                      description: providerRef references the Provider CRD.
                      properties:
                        name:
                          description: name is the name of the Provider resource.
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            namespace is the namespace of the Provider resource.
                            If not specified, the same namespace as the AgentRuntime is used.
                          type: string
                      required:
                      - name
                      type: object
                    requiredCapabilities:
                      description: |-
                        requiredCapabilities lists capabilities the provider must support for
                        this binding. If the provider does not advertise all listed capabilities,
                        the AgentRuntime enters a Pending phase with a descriptive condition.
                      items:
                        description: ProviderCapability defines a capability that
                          a provider supports.
                        enum:
                        - text
                        - streaming
                        - vision
                        - tools
                        - json
                        - audio
                        - video
                        - documents
                        - duplex
                        type: string
                      type: array
                    role:
                      default: llm
                      description: |-
                        role declares the role the AgentRuntime expects the referenced
                        Provider to fulfil. The controller asserts at reconcile time that
                        the referenced Provider's `spec.role` matches; mismatch puts the
                        AgentRuntime in Phase=Error with ProvidersReady=False.

                        Defaults to 'llm' for back-compat with existing AgentRuntimes
                        (which were authored before per-ref roles existed).
                      enum:
                      - llm
                      - embedding
                      - tts
                      - stt
                      - image
                      - inference
                      type: string
                  required:
                  - name
                  - providerRef
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              rollout:
                description: |-
                  rollout configures a progressive delivery rollout for this AgentRuntime.
                  When nil, no rollout is active and all traffic goes to the current spec.
                properties:
                  candidate:
                    description: |-
                      candidate defines the sparse overrides for the candidate version.
                      When nil, no rollout is active. Only fields that differ from the
                      stable version need to be set.
                    properties:
                      promptPackRef:
                        description: |-
                          promptPackRef overrides the PromptPack for the candidate. When set, the
                          candidate Deployment runs a different PromptPack than stable — the operator
                          resolves this reference and mounts that pack's content into the candidate
                          pods, so a new prompt can be shipped as the candidate.

                          PromptPacks are resolved by name (each version is its own PromptPack
                          resource), so to run different prompt content the candidate must reference
                          a different PromptPack name, not merely a version.
                        properties:
                          name:
                            description: name is the name of the PromptPack resource.
                            minLength: 1
                            type: string
                          track:
                            description: |-
                              track follows a release channel instead of pinning a version:
                              "stable" selects the highest non-prerelease version; "prerelease" selects
                              the highest version overall. Mutually exclusive with version.
                            enum:
                            - stable
                            - prerelease
                            type: string
                          version:
                            description: version pins an exact PromptPack version.
                              Mutually exclusive with track.
                            type: string
                        required:
                        - name
                        type: object
                        x-kubernetes-validations:
                        - message: promptPackRef.version and promptPackRef.track are
                            mutually exclusive
                          rule: '!(has(self.version) && has(self.track))'
                      providerRefs:
                        description: |-
                          providerRefs overrides the provider list for the candidate.
                          Use this to test a different LLM model or provider configuration.
                        items:
                          description: |-
                            NamedProviderRef associates a name with a Provider CRD reference.
                            The name is used to look up providers by role (e.g. "default", "judge").
                          properties:
                            name:
                              description: name is the logical name for this provider
                                (e.g. "default", "judge", "embeddings").
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                            providerRef:
                              description: providerRef references the Provider CRD.
                              properties:
                                name:
                                  description: name is the name of the Provider resource.
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: |-
                                    namespace is the namespace of the Provider resource.
                                    If not specified, the same namespace as the AgentRuntime is used.
                                  type: string
                              required:
                              - name
                              type: object
                            requiredCapabilities:
                              description: |-
                                requiredCapabilities lists capabilities the provider must support for
                                this binding. If the provider does not advertise all listed capabilities,
                                the AgentRuntime enters a Pending phase with a descriptive condition.
                              items:
                                description: ProviderCapability defines a capability
                                  that a provider supports.
                                enum:
                                - text
                                - streaming
                                - vision
                                - tools
                                - json
                                - audio
                                - video
                                - documents
                                - duplex
                                type: string
                              type: array
                            role:
                              default: llm
                              description: |-
                                role declares the role the AgentRuntime expects the referenced
                                Provider to fulfil. The controller asserts at reconcile time that
                                the referenced Provider's `spec.role` matches; mismatch puts the
                                AgentRuntime in Phase=Error with ProvidersReady=False.

                                Defaults to 'llm' for back-compat with existing AgentRuntimes
                                (which were authored before per-ref roles existed).
                              enum:
                              - llm
                              - embedding
                              - tts
                              - stt
                              - image
                              - inference
                              type: string
                          required:
                          - name
                          - providerRef
                          type: object
                        type: array
                      toolRegistryRef:
                        description: toolRegistryRef overrides the tool registry for
                          the candidate.
                        properties:
                          name:
                            description: name is the name of the ToolRegistry resource.
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              namespace is the namespace of the ToolRegistry resource.
                              If not specified, the same namespace as the AgentRuntime is used.

                              Cross-namespace references are NOT supported: it must either be omitted
                              or equal the AgentRuntime's own namespace. The agent pod's Role is
                              namespace-scoped, so a foreign namespace is unreadable from the pod and
                              registry-scoped ToolPolicies would not match. An AgentRuntime declaring
                              one is rejected with ToolRegistryCrossNamespace.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  rollback:
                    description: rollback configures automatic or manual rollback
                      behavior.
                    properties:
                      cooldown:
                        default: 5m
                        description: |-
                          cooldown is the minimum time to wait before allowing another rollout
                          after a rollback. Uses Go duration format (e.g., "5m", "1h").
                        type: string
                      mode:
                        default: manual
                        description: |-
                          mode controls how rollback is triggered.
                          "automatic" rolls back on analysis failure or error thresholds.
                          "manual" requires explicit operator action.
                          "disabled" prevents any rollback.
                        enum:
                        - automatic
                        - manual
                        - disabled
                        type: string
                    type: object
                  steps:
                    description: |-
                      steps defines the sequence of rollout actions to execute.
                      Each step sets traffic weight, introduces a pause, or triggers analysis.
                    items:
                      description: |-
                        RolloutStep is a union type — exactly one field should be set per step.
                        A sequence of steps defines the rollout shape (canary, blue-green, etc.).
                      properties:
                        analysis:
                          description: |-
                            analysis triggers a RolloutAnalysis evaluation at this step.
                            The rollout only advances if the analysis passes.
                          properties:
                            args:
                              description: args provides argument overrides for the
                                analysis template.
                              items:
                                description: AnalysisArg is a name/value pair passed
                                  to a RolloutAnalysis template.
                                properties:
                                  name:
                                    description: name is the argument name.
                                    minLength: 1
                                    type: string
                                  value:
                                    description: value is the argument value.
                                    type: string
                                required:
                                - name
                                - value
                                type: object
                              type: array
                            templateName:
                              description: |-
                                templateName is the name of the RolloutAnalysis CRD to instantiate.
                                The analysis runs in the same namespace as the AgentRuntime.
                              minLength: 1
                              type: string
                          required:
                          - templateName
                          type: object
                        pause:
                          description: |-
                            pause introduces a hold point in the rollout.
                            If duration is set, the controller waits that long before advancing.
                            If duration is nil, the rollout waits indefinitely for manual promotion.
                          properties:
                            duration:
                              description: |-
                                duration is the time to wait before automatically advancing to the next step.
                                Uses Go duration format (e.g., "5m", "1h").
                                When nil, the rollout pauses indefinitely and requires manual promotion.
                              type: string
                          type: object
                        setWeight:
                          description: setWeight sets the percentage of traffic sent
                            to the candidate version.
                          format: int32
                          maximum: 100
                          minimum: 0
                          type: integer
                      type: object
                    minItems: 1
                    type: array
                  stickySession:
                    description: |-
                      stickySession configures consistent hashing so a given user always
                      reaches the same version during a rollout.
                    properties:
                      hashOn:
                        description: |-
                          hashOn is the HTTP header name used as the consistent hashing key.
                          Typically "x-user-id". Requests with the same header value are always
                          routed to the same version.
                        minLength: 1
                        type: string
                    required:
                    - hashOn
                    type: object
                  trafficRouting:
                    description: trafficRouting configures integration with a service
                      mesh for traffic splitting.
                    properties:
                      istio:
                        description: |-
                          istio is the legacy reference-existing form (mode: external). The operator
                          patches weights on a VS + DR the user authored.
                        properties:
                          destinationRule:
                            description: destinationRule references the Istio DestinationRule
                              to patch.
                            properties:
                              candidateSubset:
                                default: canary
                                description: candidateSubset is the subset name for
                                  candidate (new) traffic.
                                type: string
                              name:
                                description: name is the DestinationRule resource
                                  name.
                                minLength: 1
                                type: string
                              stableSubset:
                                default: stable
                                description: stableSubset is the subset name for stable
                                  (current) traffic.
                                type: string
                            required:
                            - name
                            type: object
                          virtualService:
                            description: virtualService references the Istio VirtualService
                              to patch.
                            properties:
                              name:
                                description: name is the VirtualService resource name.
                                minLength: 1
                                type: string
                              routes:
                                description: routes lists the HTTP route names within
                                  the VirtualService to patch.
                                items:
                                  type: string
                                minItems: 1
                                type: array
                            required:
                            - name
                            - routes
                            type: object
                        required:
                        - destinationRule
                        - virtualService
                        type: object
                      mesh:
                        description: |-
                          mesh configures operator-OWNED VirtualService + DestinationRule routing
                          (mode: mesh). Subsets are derived from the operator's own track labels.
                        properties:
                          candidateSubset:
                            default: canary
                            type: string
                          hosts:
                            description: |-
                              hosts the VirtualService matches. Empty defaults to the agent's own
                              Service DNS name (the east-west self-play path).
                            items:
                              type: string
                            type: array
                          stableSubset:
                            default: stable
                            description: stableSubset / candidateSubset name the DestinationRule
                              subsets.
                            type: string
                          waypoint:
                            description: |-
                              waypoint is the name of the Istio ambient waypoint (a gateway-api Gateway
                              of class istio-waypoint) that performs the L7 weighted split. When set, the
                              operator stamps istio.io/use-waypoint=<waypoint> on the agent Service so its
                              traffic is routed through the waypoint — without this, ambient ztunnel stays
                              L4-only and the operator-owned VirtualService is silently bypassed. Empty
                              leaves the Service unenrolled (no waypoint available).
                            type: string
                        type: object
                      mode:
                        description: |-
                          mode selects the delivery mechanism. Unset auto-resolves: "mesh" when the
                          mesh is available, else "replicaWeighted". A legacy config that sets
                          `istio` without `mode` resolves to "external".
                        enum:
                        - mesh
                        - replicaWeighted
                        - external
                        type: string
                    type: object
                  trigger:
                    description: |-
                      trigger opts this agent into version-triggered canary rollouts: when a
                      newer PromptPack version appears on the named channel, the controller
                      sets it as the rollout candidate and the steps above canary it.
                    properties:
                      promptPackChannel:
                        description: promptPackChannel is the release channel to watch
                          for newer versions.
                        enum:
                        - stable
                        - prerelease
                        type: string
                    required:
                    - promptPackChannel
                    type: object
                required:
                - steps
                type: object
              runtime:
                description: runtime configures deployment settings like replicas
                  and resources.
                properties:
                  affinity:
                    description: affinity defines affinity rules for pod scheduling.
                    properties:
                      nodeAffinity:
                        description: Describes node affinity scheduling rules for
                          the pod.
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node matches the corresponding matchExpressions; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: |-
                                An empty preferred scheduling term matches all objects with implicit weight 0
                                (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                              properties:
                                preference:
                                  description: A node selector term, associated with
                                    the corresponding weight.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                weight:
                                  description: Weight associated with matching the
                                    corresponding nodeSelectorTerm, in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - preference
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to an update), the system
                              may or may not try to eventually evict the pod from its node.
                            properties:
                              nodeSelectorTerms:
                                description: Required. A list of node selector terms.
                                  The terms are ORed.
                                items:
                                  description: |-
                                    A null or empty node selector term matches no objects. The requirements of
                                    them are ANDed.
                                    The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - nodeSelectorTerms
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      podAffinity:
                        description: Describes pod affinity scheduling rules (e.g.
                          co-locate this pod in the same node, zone, etc. as some
                          other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      podAntiAffinity:
                        description: Describes pod anti-affinity scheduling rules
                          (e.g. avoid putting this pod in the same node, zone, etc.
                          as some other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the anti-affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling anti-affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and subtracting
                              "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the anti-affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the anti-affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                    type: object
                  autoscaling:
                    description: autoscaling configures horizontal pod autoscaling
                      for the agent.
                    properties:
                      enabled:
                        default: false
                        description: |-
                          enabled specifies whether autoscaling is enabled.
                          When enabled, the autoscaler will manage replica count instead of spec.runtime.replicas.
                        type: boolean
                      keda:
                        description: keda contains KEDA-specific configuration. Only
                          used when type is "keda".
                        properties:
                          connectionThreshold:
                            description: |-
                              connectionThreshold is the target number of active connections per pod
                              for the default KEDA Prometheus trigger. Only used when triggers is empty.
                              Defaults to 200 for text workloads. Set lower (e.g., 20) for audio/media workloads.
                            format: int32
                            minimum: 1
                            type: integer
                          cooldownPeriod:
                            default: 300
                            description: cooldownPeriod is the wait period in seconds
                              after last trigger before scaling down. Defaults to
                              300.
                            format: int32
                            minimum: 0
                            type: integer
                          pollingInterval:
                            default: 30
                            description: pollingInterval is the interval in seconds
                              to check triggers. Defaults to 30.
                            format: int32
                            minimum: 1
                            type: integer
                          triggers:
                            description: |-
                              triggers is the list of KEDA triggers for scaling.
                              If empty, a default Prometheus trigger for connections is configured.
                            items:
                              description: KEDATrigger defines a KEDA scaling trigger.
                              properties:
                                metadata:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    metadata contains trigger-specific configuration.
                                    For prometheus: serverAddress, query, threshold
                                    For cron: timezone, start, end, desiredReplicas
                                  type: object
                                type:
                                  description: type is the KEDA trigger type (e.g.,
                                    "prometheus", "cron").
                                  type: string
                              required:
                              - metadata
                              - type
                              type: object
                            type: array
                        type: object
                      maxReplicas:
                        default: 100
                        description: maxReplicas is the maximum number of replicas.
                        format: int32
                        minimum: 1
                        type: integer
                      minReplicas:
                        default: 1
                        description: |-
                          minReplicas is the minimum number of replicas.
                          For KEDA, set to 0 to enable scale-to-zero.
                        format: int32
                        minimum: 0
                        type: integer
                      scaleDownStabilizationSeconds:
                        default: 300
                        description: |-
                          scaleDownStabilizationSeconds is the number of seconds to wait before
                          scaling down after a scale-up. This prevents thrashing when connections
                          are bursty. Defaults to 300 (5 minutes). Only used for HPA type.
                        format: int32
                        maximum: 3600
                        minimum: 0
                        type: integer
                      targetCPUUtilizationPercentage:
                        default: 90
                        description: |-
                          targetCPUUtilizationPercentage is the target average CPU utilization.
                          CPU is a secondary metric since agents are typically I/O bound.
                          Set to nil to disable CPU-based scaling. Defaults to 90% as a safety valve.
                          Only used for HPA type.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                      targetMemoryUtilizationPercentage:
                        default: 70
                        description: |-
                          targetMemoryUtilizationPercentage is the target average memory utilization.
                          Memory is the primary scaling metric since each WebSocket connection and
                          session consumes memory. Defaults to 70%. Only used for HPA type.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                      type:
                        default: hpa
                        description: |-
                          type specifies which autoscaler to use. Defaults to "hpa".
                          Use "keda" for advanced scaling (scale to zero, Prometheus metrics, cron).
                        enum:
                        - hpa
                        - keda
                        type: string
                    type: object
                  extraEnv:
                    description: |-
                      extraEnv defines additional environment variables for the runtime container.
                      Use this for debugging (e.g., LOG_LEVEL=debug) or custom configuration.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector is a map of node labels for pod scheduling.
                    type: object
                  replicas:
                    default: 1
                    description: |-
                      replicas is the desired number of agent runtime pods.
                      This field is ignored when autoscaling is enabled.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: resources defines compute resource requirements for
                      the agent container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: tolerations are tolerations for pod scheduling.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  volumeMounts:
                    description: |-
                      volumeMounts defines additional volume mounts for the runtime container.
                      Each mount must reference a volume defined in the volumes field.
                    items:
                      description: VolumeMount describes a mounting of a Volume within
                        a container.
                      properties:
                        mountPath:
                          description: |-
                            Path within the container at which the volume should be mounted.  Must
                            not contain ':'.
                          type: string
                        mountPropagation:
                          description: |-
                            mountPropagation determines how mounts are propagated from the host
                            to container and the other way around.
                            When not set, MountPropagationNone is used.
                            This field is beta in 1.10.
                            When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                            (which defaults to None).
                          type: string
                        name:
                          description: This must match the Name of a Volume.
                          type: string
                        readOnly:
                          description: |-
                            Mounted read-only if true, read-write otherwise (false or unspecified).
                            Defaults to false.
                          type: boolean
                        recursiveReadOnly:
                          description: |-
                            RecursiveReadOnly specifies whether read-only mounts should be handled
                            recursively.

                            If ReadOnly is false, this field has no meaning and must be unspecified.

                            If ReadOnly is true, and this field is set to Disabled, the mount is not made
                            recursively read-only.  If this field is set to IfPossible, the mount is made
                            recursively read-only, if it is supported by the container runtime.  If this
                            field is set to Enabled, the mount is made recursively read-only if it is
                            supported by the container runtime, otherwise the pod will not be started and
                            an error will be generated to indicate the reason.

                            If this field is set to IfPossible or Enabled, MountPropagation must be set to
                            None (or be unspecified, which defaults to None).

                            If this field is not specified, it is treated as an equivalent of Disabled.
                          type: string
                        subPath:
                          description: |-
                            Path within the volume from which the container's volume should be mounted.
                            Defaults to "" (volume's root).
                          type: string
                        subPathExpr:
                          description: |-
                            Expanded path within the volume from which the container's volume should be mounted.
                            Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                            Defaults to "" (volume's root).
                            SubPathExpr and SubPath are mutually exclusive.
                          type: string
                      required:
                      - mountPath
                      - name
                      type: object
                    type: array
                  volumes:
                    description: |-
                      volumes defines additional volumes to mount in the runtime pod.
                      Use this to mount PVCs, ConfigMaps, or Secrets for media files or mock configurations.
                    items:
                      description: Volume represents a named volume in a pod that
                        may be accessed by any container in the pod.
                      properties:
                        awsElasticBlockStore:
                          description: |-
                            awsElasticBlockStore represents an AWS Disk resource that is attached to a
                            kubelet's host machine and then exposed to the pod.
                            Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree
                            awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.
                            More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                          properties:
                            fsType:
                              description: |-
                                fsType is the filesystem type of the volume that you want to mount.
                                Tip: Ensure that the filesystem type is supported by the host operating system.
                                Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                              type: string
                            partition:
                              description: |-
                                partition is the partition in the volume that you want to mount.
                                If omitted, the default is to mount by volume name.
                                Examples: For volume /dev/sda1, you specify the partition as "1".
                                Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).
                              format: int32
                              type: integer
                            readOnly:
                              description: |-
                                readOnly value true will force the readOnly setting in VolumeMounts.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                              type: boolean
                            volumeID:
                              description: |-
                                volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                              type: string
                          required:
                          - volumeID
                          type: object
                        azureDisk:
                          description: |-
                            azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.
                            Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type
                            are redirected to the disk.csi.azure.com CSI driver.
                          properties:
                            cachingMode:
                              description: 'cachingMode is the Host Caching mode:
                                None, Read Only, Read Write.'
                              type: string
                            diskName:
                              description: diskName is the Name of the data disk in
                                the blob storage
                              type: string
                            diskURI:
                              description: diskURI is the URI of data disk in the
                                blob storage
                              type: string
                            fsType:
                              default: ext4
                              description: |-
                                fsType is Filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                              type: string
                            kind:
                              description: 'kind expected values are Shared: multiple
                                blob disks per storage account  Dedicated: single
                                blob disk per storage account  Managed: azure managed
                                data disk (only in managed availability set). defaults
                                to shared'
                              type: string
                            readOnly:
                              default: false
                              description: |-
                                readOnly Defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                          required:
                          - diskName
                          - diskURI
                          type: object
                        azureFile:
                          description: |-
                            azureFile represents an Azure File Service mount on the host and bind mount to the pod.
                            Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type
                            are redirected to the file.csi.azure.com CSI driver.
                          properties:
                            readOnly:
                              description: |-
                                readOnly defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                            secretName:
                              description: secretName is the  name of secret that
                                contains Azure Storage Account Name and Key
                              type: string
                            shareName:
                              description: shareName is the azure share Name
                              type: string
                          required:
                          - secretName
                          - shareName
                          type: object
                        cephfs:
                          description: |-
                            cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.
                            Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.
                          properties:
                            monitors:
                              description: |-
                                monitors is Required: Monitors is a collection of Ceph monitors
                                More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            path:
                              description: 'path is Optional: Used as the mounted
                                root, rather than the full Ceph tree, default is /'
                              type: string
                            readOnly:
                              description: |-
                                readOnly is Optional: Defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                                More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                              type: boolean
                            secretFile:
                              description: |-
                                secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret
                                More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                              type: string
                            secretRef:
                              description: |-
                                secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.
                                More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            user:
                              description: |-
                                user is optional: User is the rados user name, default is admin
                                More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                              type: string
                          required:
                          - monitors
                          type: object
                        cinder:
                          description: |-
                            cinder represents a cinder volume attached and mounted on kubelets host machine.
                            Deprecated: Cinder is deprecated. All operations for the in-tree cinder type
                            are redirected to the cinder.csi.openstack.org CSI driver.
                            More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                          properties:
                            fsType:
                              description: |-
                                fsType is the filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                              type: string
                            readOnly:
                              description: |-
                                readOnly defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                                More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                              type: boolean
                            secretRef:
                              description: |-
                                secretRef is optional: points to a secret object containing parameters used to connect
                                to OpenStack.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            volumeID:
                              description: |-
                                volumeID used to identify the volume in cinder.
                                More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                              type: string
                          required:
                          - volumeID
                          type: object
                        configMap:
                          description: configMap represents a configMap that should
                            populate this volume
                          properties:
                            defaultMode:
                              description: |-
                                defaultMode is optional: mode bits used to set permissions on created files by default.
                                Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                Defaults to 0644.
                                Directories within the path are not affected by this setting.
                                This might be in conflict with other options that affect the file
                                mode, like fsGroup, and the result can be other mode bits set.
                              format: int32
                              type: integer
                            items:
                              description: |-
                                items if unspecified, each key-value pair in the Data field of the referenced
                                ConfigMap will be projected into the volume as a file whose name is the
                                key and content is the value. If specified, the listed keys will be
                                projected into the specified paths, and unlisted keys will not be
                                present. If a key is specified which is not present in the ConfigMap,
                                the volume setup will error unless it is marked optional. Paths must be
                                relative and may not contain the '..' path or start with '..'.
                              items:
                                description: Maps a string key to a path within a
                                  volume.
                                properties:
                                  key:
                                    description: key is the key to project.
                                    type: string
                                  mode:
                                    description: |-
                                      mode is Optional: mode bits used to set permissions on this file.
                                      Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                      YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                      If not specified, the volume defaultMode will be used.
                                      This might be in conflict with other options that affect the file
                                      mode, like fsGroup, and the result can be other mode bits set.
                                    format: int32
                                    type: integer
                                  path:
                                    description: |-
                                      path is the relative path of the file to map the key to.
                                      May not be an absolute path.
                                      May not contain the path element '..'.
                                      May not start with the string '..'.
                                    type: string
                                required:
                                - key
                                - path
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: optional specify whether the ConfigMap
                                or its keys must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                        csi:
                          description: csi (Container Storage Interface) represents
                            ephemeral storage that is handled by certain external
                            CSI drivers.
                          properties:
                            driver:
                              description: |-
                                driver is the name of the CSI driver that handles this volume.
                                Consult with your admin for the correct name as registered in the cluster.
                              type: string
                            fsType:
                              description: |-
                                fsType to mount. Ex. "ext4", "xfs", "ntfs".
                                If not provided, the empty value is passed to the associated CSI driver
                                which will determine the default filesystem to apply.
                              type: string
                            nodePublishSecretRef:
                              description: |-
                                nodePublishSecretRef is a reference to the secret object containing
                                sensitive information to pass to the CSI driver to complete the CSI
                                NodePublishVolume and NodeUnpublishVolume calls.
                                This field is optional, and  may be empty if no secret is required. If the
                                secret object contains more than one secret, all secret references are passed.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            readOnly:
                              description: |-
                                readOnly specifies a read-only configuration for the volume.
                                Defaults to false (read/write).
                              type: boolean
                            volumeAttributes:
                              additionalProperties:
                                type: string
                              description: |-
                                volumeAttributes stores driver-specific properties that are passed to the CSI
                                driver. Consult your driver's documentation for supported values.
                              type: object
                          required:
                          - driver
                          type: object
                        downwardAPI:
                          description: downwardAPI represents downward API about the
                            pod that should populate this volume
                          properties:
                            defaultMode:
                              description: |-
                                Optional: mode bits to use on created files by default. Must be a
                                Optional: mode bits used to set permissions on created files by default.
                                Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                Defaults to 0644.
                                Directories within the path are not affected by this setting.
                                This might be in conflict with other options that affect the file
                                mode, like fsGroup, and the result can be other mode bits set.
                              format: int32
                              type: integer
                            items:
                              description: Items is a list of downward API volume
                                file
                              items:
                                description: DownwardAPIVolumeFile represents information
                                  to create the file containing the pod field
                                properties:
                                  fieldRef:
                                    description: 'Required: Selects a field of the
                                      pod: only annotations, labels, name, namespace
                                      and uid are supported.'
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  mode:
                                    description: |-
                                      Optional: mode bits used to set permissions on this file, must be an octal value
                                      between 0000 and 0777 or a decimal value between 0 and 511.
                                      YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                      If not specified, the volume defaultMode will be used.
                                      This might be in conflict with other options that affect the file
                                      mode, like fsGroup, and the result can be other mode bits set.
                                    format: int32
                                    type: integer
                                  path:
                                    description: 'Required: Path is  the relative
                                      path name of the file to be created. Must not
                                      be absolute or contain the ''..'' path. Must
                                      be utf-8 encoded. The first item of the relative
                                      path must not start with ''..'''
                                    type: string
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                required:
                                - path
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                        emptyDir:
                          description: |-
                            emptyDir represents a temporary directory that shares a pod's lifetime.
                            More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                          properties:
                            medium:
                              description: |-
                                medium represents what type of storage medium should back this directory.
                                The default is "" which means to use the node's default medium.
                                Must be an empty string (default) or Memory.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                              type: string
                            sizeLimit:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                sizeLimit is the total amount of local storage required for this EmptyDir volume.
                                The size limit is also applicable for memory medium.
                                The maximum usage on memory medium EmptyDir would be the minimum value between
                                the SizeLimit specified here and the sum of memory limits of all containers in a pod.
                                The default is nil which means that the limit is undefined.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          type: object
                        ephemeral:
                          description: |-
                            ephemeral represents a volume that is handled by a cluster storage driver.
                            The volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,
                            and deleted when the pod is removed.

                            Use this if:
                            a) the volume is only needed while the pod runs,
                            b) features of normal volumes like restoring from snapshot or capacity
                               tracking are needed,
                            c) the storage driver is specified through a storage class, and
                            d) the storage driver supports dynamic volume provisioning through
                               a PersistentVolumeClaim (see EphemeralVolumeSource for more
                               information on the connection between this volume type
                               and PersistentVolumeClaim).

                            Use PersistentVolumeClaim or one of the vendor-specific
                            APIs for volumes that persist for longer than the lifecycle
                            of an individual pod.

                            Use CSI for light-weight local ephemeral volumes if the CSI driver is meant to
                            be used that way - see the documentation of the driver for
                            more information.

                            A pod can use both types of ephemeral volumes and
                            persistent volumes at the same time.
                          properties:
                            volumeClaimTemplate:
                              description: |-
                                Will be used to create a stand-alone PVC to provision the volume.
                                The pod in which this EphemeralVolumeSource is embedded will be the
                                owner of the PVC, i.e. the PVC will be deleted together with the
                                pod.  The name of the PVC will be `<pod name>-<volume name>` where
                                `<volume name>` is the name from the `PodSpec.Volumes` array
                                entry. Pod validation will reject the pod if the concatenated name
                                is not valid for a PVC (for example, too long).

                                An existing PVC with that name that is not owned by the pod
                                will *not* be used for the pod to avoid using an unrelated
                                volume by mistake. Starting the pod is then blocked until
                                the unrelated PVC is removed. If such a pre-created PVC is
                                meant to be used by the pod, the PVC has to updated with an
                                owner reference to the pod once the pod exists. Normally
                                this should not be necessary, but it may be useful when
                                manually reconstructing a broken cluster.

                                This field is read-only and no changes will be made by Kubernetes
                                to the PVC after it has been created.

                                Required, must not be nil.
                              properties:
                                metadata:
                                  description: |-
                                    May contain labels and annotations that will be copied into the PVC
                                    when creating it. No other fields are allowed and will be rejected during
                                    validation.
                                  type: object
                                spec:
                                  description: |-
                                    The specification for the PersistentVolumeClaim. The entire content is
                                    copied unchanged into the PVC that gets created from this
                                    template. The same fields as in a PersistentVolumeClaim
                                    are also valid here.
                                  properties:
                                    accessModes:
                                      description: |-
                                        accessModes contains the desired access modes the volume should have.
                                        More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    dataSource:
                                      description: |-
                                        dataSource field can be used to specify either:
                                        * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                                        * An existing PVC (PersistentVolumeClaim)
                                        If the provisioner or an external controller can support the specified data source,
                                        it will create a new volume based on the contents of the specified data source.
                                        When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                                        and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                                        If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                                      properties:
                                        apiGroup:
                                          description: |-
                                            APIGroup is the group for the resource being referenced.
                                            If APIGroup is not specified, the specified Kind must be in the core API group.
                                            For any other third-party types, APIGroup is required.
                                          type: string
                                        kind:
                                          description: Kind is the type of resource
                                            being referenced
                                          type: string
                                        name:
                                          description: Name is the name of resource
                                            being referenced
                                          type: string
                                      required:
                                      - kind
                                      - name
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    dataSourceRef:
                                      description: |-
                                        dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                                        volume is desired. This may be any object from a non-empty API group (non
                                        core object) or a PersistentVolumeClaim object.
                                        When this field is specified, volume binding will only succeed if the type of
                                        the specified object matches some installed volume populator or dynamic
                                        provisioner.
                                        This field will replace the functionality of the dataSource field and as such
                                        if both fields are non-empty, they must have the same value. For backwards
                                        compatibility, when namespace isn't specified in dataSourceRef,
                                        both fields (dataSource and dataSourceRef) will be set to the same
                                        value automatically if one of them is empty and the other is non-empty.
                                        When namespace is specified in dataSourceRef,
                                        dataSource isn't set to the same value and must be empty.
                                        There are three important differences between dataSource and dataSourceRef:
                                        * While dataSource only allows two specific types of objects, dataSourceRef
                                          allows any non-core object, as well as PersistentVolumeClaim objects.
                                        * While dataSource ignores disallowed values (dropping them), dataSourceRef
                                          preserves all values, and generates an error if a disallowed value is
                                          specified.
                                        * While dataSource only allows local objects, dataSourceRef allows objects
                                          in any namespaces.
                                        (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                                        (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                                      properties:
                                        apiGroup:
                                          description: |-
                                            APIGroup is the group for the resource being referenced.
                                            If APIGroup is not specified, the specified Kind must be in the core API group.
                                            For any other third-party types, APIGroup is required.
                                          type: string
                                        kind:
                                          description: Kind is the type of resource
                                            being referenced
                                          type: string
                                        name:
                                          description: Name is the name of resource
                                            being referenced
                                          type: string
                                        namespace:
                                          description: |-
                                            Namespace is the namespace of resource being referenced
                                            Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                                            (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                                          type: string
                                      required:
                                      - kind
                                      - name
                                      type: object
                                    resources:
                                      description: |-
                                        resources represents the minimum resources the volume should have.
                                        Users are allowed to specify resource requirements
                                        that are lower than previous value but must still be higher than capacity recorded in the
                                        status field of the claim.
                                        More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                                      properties:
                                        limits:
                                          additionalProperties:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          description: |-
                                            Limits describes the maximum amount of compute resources allowed.
                                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                          type: object
                                        requests:
                                          additionalProperties:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          description: |-
                                            Requests describes the minimum amount of compute resources required.
                                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                          type: object
                                      type: object
                                    selector:
                                      description: selector is a label query over
                                        volumes to consider for binding.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    storageClassName:
                                      description: |-
                                        storageClassName is the name of the StorageClass required by the claim.
                                        More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                                      type: string
                                    volumeAttributesClassName:
                                      description: |-
                                        volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                                        If specified, the CSI driver will create or update the volume with the attributes defined
                                        in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                                        it can be changed after the claim is created. An empty string or nil value indicates that no
                                        VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                                        this field can be reset to its previous value (including nil) to cancel the modification.
                                        If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                                        set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                                        exists.
                                        More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                                      type: string
                                    volumeMode:
                                      description: |-
                                        volumeMode defines what type of volume is required by the claim.
                                        Value of Filesystem is implied when not included in claim spec.
                                      type: string
                                    volumeName:
                                      description: volumeName is the binding reference
                                        to the PersistentVolume backing this claim.
                                      type: string
                                  type: object
                              required:
                              - spec
                              type: object
                          type: object
                        fc:
                          description: fc represents a Fibre Channel resource that
                            is attached to a kubelet's host machine and then exposed
                            to the pod.
                          properties:
                            fsType:
                              description: |-
                                fsType is the filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                              type: string
                            lun:
                              description: 'lun is Optional: FC target lun number'
                              format: int32
                              type: integer
                            readOnly:
                              description: |-
                                readOnly is Optional: Defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                            targetWWNs:
                              description: 'targetWWNs is Optional: FC target worldwide
                                names (WWNs)'
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            wwids:
                              description: |-
                                wwids Optional: FC volume world wide identifiers (wwids)
                                Either wwids or combination of targetWWNs and lun must be set, but not both simultaneously.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                        flexVolume:
                          description: |-
                            flexVolume represents a generic volume resource that is
                            provisioned/attached using an exec based plugin.
                            Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.
                          properties:
                            driver:
                              description: driver is the name of the driver to use
                                for this volume.
                              type: string
                            fsType:
                              description: |-
                                fsType is the filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.
                              type: string
                            options:
                              additionalProperties:
                                type: string
                              description: 'options is Optional: this field holds
                                extra command options if any.'
                              type: object
                            readOnly:
                              description: |-
                                readOnly is Optional: defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                            secretRef:
                              description: |-
                                secretRef is Optional: secretRef is reference to the secret object containing
                                sensitive information to pass to the plugin scripts. This may be
                                empty if no secret object is specified. If the secret object
                                contains more than one secret, all secrets are passed to the plugin
                                scripts.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - driver
                          type: object
                        flocker:
                          description: |-
                            flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.
                            Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.
                          properties:
                            datasetName:
                              description: |-
                                datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker
                                should be considered as deprecated
                              type: string
                            datasetUUID:
                              description: datasetUUID is the UUID of the dataset.
                                This is unique identifier of a Flocker dataset
                              type: string
                          type: object
                        gcePersistentDisk:
                          description: |-
                            gcePersistentDisk represents a GCE Disk resource that is attached to a
                            kubelet's host machine and then exposed to the pod.
                            Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree
                            gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.
                            More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                          properties:
                            fsType:
                              description: |-
                                fsType is filesystem type of the volume that you want to mount.
                                Tip: Ensure that the filesystem type is supported by the host operating system.
                                Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                              type: string
                            partition:
                              description: |-
                                partition is the partition in the volume that you want to mount.
                                If omitted, the default is to mount by volume name.
                                Examples: For volume /dev/sda1, you specify the partition as "1".
                                Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                              format: int32
                              type: integer
                            pdName:
                              description: |-
                                pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                              type: string
                            readOnly:
                              description: |-
                                readOnly here will force the ReadOnly setting in VolumeMounts.
                                Defaults to false.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                              type: boolean
                          required:
                          - pdName
                          type: object
                        gitRepo:
                          description: |-
                            gitRepo represents a git repository at a particular revision.
                            Deprecated: GitRepo is deprecated. To provision a container with a git repo, mount an
                            EmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir
                            into the Pod's container.
                          properties:
                            directory:
                              description: |-
                                directory is the target directory name.
                                Must not contain or start with '..'.  If '.' is supplied, the volume directory will be the
                                git repository.  Otherwise, if specified, the volume will contain the git repository in
                                the subdirectory with the given name.
                              type: string
                            repository:
                              description: repository is the URL
                              type: string
                            revision:
                              description: revision is the commit hash for the specified
                                revision.
                              type: string
                          required:
                          - repository
                          type: object
                        glusterfs:
                          description: |-
                            glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.
                            Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.
                          properties:
                            endpoints:
                              description: endpoints is the endpoint name that details
                                Glusterfs topology.
                              type: string
                            path:
                              description: |-
                                path is the Glusterfs volume path.
                                More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod
                              type: string
                            readOnly:
                              description: |-
                                readOnly here will force the Glusterfs volume to be mounted with read-only permissions.
                                Defaults to false.
                                More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod
                              type: boolean
                          required:
                          - endpoints
                          - path
                          type: object
                        hostPath:
                          description: |-
                            hostPath represents a pre-existing file or directory on the host
                            machine that is directly exposed to the container. This is generally
                            used for system agents or other privileged things that are allowed
                            to see the host machine. Most containers will NOT need this.
                            More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
                          properties:
                            path:
                              description: |-
                                path of the directory on the host.
                                If the path is a symlink, it will follow the link to the real path.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
                              type: string
                            type:
                              description: |-
                                type for HostPath Volume
                                Defaults to ""
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
                              type: string
                          required:
                          - path
                          type: object
                        image:
                          description: |-
                            image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.
                            The volume is resolved at pod startup depending on which PullPolicy value is provided:

                            - Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.
                            - Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.
                            - IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.

                            The volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.
                            A failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.
                            The types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.
                            The OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.
                            The volume will be mounted read-only (ro).
                            Sub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.
                            The field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.
                          properties:
                            pullPolicy:
                              description: |-
                                Policy for pulling OCI objects. Possible values are:
                                Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.
                                Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.
                                IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.
                                Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.
                              type: string
                            reference:
                              description: |-
                                Required: Image or artifact reference to be used.
                                Behaves in the same way as pod.spec.containers[*].image.
                                Pull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.
                                More info: https://kubernetes.io/docs/concepts/containers/images
                                This field is optional to allow higher level config management to default or override
                                container images in workload controllers like Deployments and StatefulSets.
                              type: string
                          type: object
                        iscsi:
                          description: |-
                            iscsi represents an ISCSI Disk resource that is attached to a
                            kubelet's host machine and then exposed to the pod.
                            More info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi
                          properties:
                            chapAuthDiscovery:
                              description: chapAuthDiscovery defines whether support
                                iSCSI Discovery CHAP authentication
                              type: boolean
                            chapAuthSession:
                              description: chapAuthSession defines whether support
                                iSCSI Session CHAP authentication
                              type: boolean
                            fsType:
                              description: |-
                                fsType is the filesystem type of the volume that you want to mount.
                                Tip: Ensure that the filesystem type is supported by the host operating system.
                                Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi
                              type: string
                            initiatorName:
                              description: |-
                                initiatorName is the custom iSCSI Initiator Name.
                                If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface
                                <target portal>:<volume name> will be created for the connection.
                              type: string
                            iqn:
                              description: iqn is the target iSCSI Qualified Name.
                              type: string
                            iscsiInterface:
                              default: default
                              description: |-
                                iscsiInterface is the interface Name that uses an iSCSI transport.
                                Defaults to 'default' (tcp).
                              type: string
                            lun:
                              description: lun represents iSCSI Target Lun number.
                              format: int32
                              type: integer
                            portals:
                              description: |-
                                portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port
                                is other than default (typically TCP ports 860 and 3260).
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            readOnly:
                              description: |-
                                readOnly here will force the ReadOnly setting in VolumeMounts.
                                Defaults to false.
                              type: boolean
                            secretRef:
                              description: secretRef is the CHAP Secret for iSCSI
                                target and initiator authentication
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            targetPortal:
                              description: |-
                                targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port
                                is other than default (typically TCP ports 860 and 3260).
                              type: string
                          required:
                          - iqn
                          - lun
                          - targetPortal
                          type: object
                        name:
                          description: |-
                            name of the volume.
                            Must be a DNS_LABEL and unique within the pod.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        nfs:
                          description: |-
                            nfs represents an NFS mount on the host that shares a pod's lifetime
                            More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                          properties:
                            path:
                              description: |-
                                path that is exported by the NFS server.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                              type: string
                            readOnly:
                              description: |-
                                readOnly here will force the NFS export to be mounted with read-only permissions.
                                Defaults to false.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                              type: boolean
                            server:
                              description: |-
                                server is the hostname or IP address of the NFS server.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                              type: string
                          required:
                          - path
                          - server
                          type: object
                        persistentVolumeClaim:
                          description: |-
                            persistentVolumeClaimVolumeSource represents a reference to a
                            PersistentVolumeClaim in the same namespace.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
                          properties:
                            claimName:
                              description: |-
                                claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.
                                More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
                              type: string
                            readOnly:
                              description: |-
                                readOnly Will force the ReadOnly setting in VolumeMounts.
                                Default false.
                              type: boolean
                          required:
                          - claimName
                          type: object
                        photonPersistentDisk:
                          description: |-
                            photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.
                            Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.
                          properties:
                            fsType:
                              description: |-
                                fsType is the filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                              type: string
                            pdID:
                              description: pdID is the ID that identifies Photon Controller
                                persistent disk
                              type: string
                          required:
                          - pdID
                          type: object
                        portworxVolume:
                          description: |-
                            portworxVolume represents a portworx volume attached and mounted on kubelets host machine.
                            Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type
                            are redirected to the pxd.portworx.com CSI driver.
                          properties:
                            fsType:
                              description: |-
                                fSType represents the filesystem type to mount
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs". Implicitly inferred to be "ext4" if unspecified.
                              type: string
                            readOnly:
                              description: |-
                                readOnly defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                            volumeID:
                              description: volumeID uniquely identifies a Portworx
                                volume
                              type: string
                          required:
                          - volumeID
                          type: object
                        projected:
                          description: projected items for all in one resources secrets,
                            configmaps, and downward API
                          properties:
                            defaultMode:
                              description: |-
                                defaultMode are the mode bits used to set permissions on created files by default.
                                Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                Directories within the path are not affected by this setting.
                                This might be in conflict with other options that affect the file
                                mode, like fsGroup, and the result can be other mode bits set.
                              format: int32
                              type: integer
                            sources:
                              description: |-
                                sources is the list of volume projections. Each entry in this list
                                handles one source.
                              items:
                                description: |-
                                  Projection that may be projected along with other supported volume types.
                                  Exactly one of these fields must be set.
                                properties:
                                  clusterTrustBundle:
                                    description: |-
                                      ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field
                                      of ClusterTrustBundle objects in an auto-updating file.

                                      Alpha, gated by the ClusterTrustBundleProjection feature gate.

                                      ClusterTrustBundle objects can either be selected by name, or by the
                                      combination of signer name and a label selector.

                                      Kubelet performs aggressive normalization of the PEM contents written
                                      into the pod filesystem.  Esoteric PEM features such as inter-block
                                      comments and block headers are stripped.  Certificates are deduplicated.
                                      The ordering of certificates within the file is arbitrary, and Kubelet
                                      may change the order over time.
                                    properties:
                                      labelSelector:
                                        description: |-
                                          Select all ClusterTrustBundles that match this label selector.  Only has
                                          effect if signerName is set.  Mutually-exclusive with name.  If unset,
                                          interpreted as "match nothing".  If set but empty, interpreted as "match
                                          everything".
                                        properties:
                                          matchExpressions:
                                            description: matchExpressions is a list
                                              of label selector requirements. The
                                              requirements are ANDed.
                                            items:
                                              description: |-
                                                A label selector requirement is a selector that contains values, a key, and an operator that
                                                relates the key and values.
                                              properties:
                                                key:
                                                  description: key is the label key
                                                    that the selector applies to.
                                                  type: string
                                                operator:
                                                  description: |-
                                                    operator represents a key's relationship to a set of values.
                                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                                  type: string
                                                values:
                                                  description: |-
                                                    values is an array of string values. If the operator is In or NotIn,
                                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                    the values array must be empty. This array is replaced during a strategic
                                                    merge patch.
                                                  items:
                                                    type: string
                                                  type: array
                                                  x-kubernetes-list-type: atomic
                                              required:
                                              - key
                                              - operator
                                              type: object
                                            type: array
                                            x-kubernetes-list-type: atomic
                                          matchLabels:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                                            type: object
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      name:
                                        description: |-
                                          Select a single ClusterTrustBundle by object name.  Mutually-exclusive
                                          with signerName and labelSelector.
                                        type: string
                                      optional:
                                        description: |-
                                          If true, don't block pod startup if the referenced ClusterTrustBundle(s)
                                          aren't available.  If using name, then the named ClusterTrustBundle is
                                          allowed not to exist.  If using signerName, then the combination of
                                          signerName and labelSelector is allowed to match zero
                                          ClusterTrustBundles.
                                        type: boolean
                                      path:
                                        description: Relative path from the volume
                                          root to write the bundle.
                                        type: string
                                      signerName:
                                        description: |-
                                          Select all ClusterTrustBundles that match this signer name.
                                          Mutually-exclusive with name.  The contents of all selected
                                          ClusterTrustBundles will be unified and deduplicated.
                                        type: string
                                    required:
                                    - path
                                    type: object
                                  configMap:
                                    description: configMap information about the configMap
                                      data to project
                                    properties:
                                      items:
                                        description: |-
                                          items if unspecified, each key-value pair in the Data field of the referenced
                                          ConfigMap will be projected into the volume as a file whose name is the
                                          key and content is the value. If specified, the listed keys will be
                                          projected into the specified paths, and unlisted keys will not be
                                          present. If a key is specified which is not present in the ConfigMap,
                                          the volume setup will error unless it is marked optional. Paths must be
                                          relative and may not contain the '..' path or start with '..'.
                                        items:
                                          description: Maps a string key to a path
                                            within a volume.
                                          properties:
                                            key:
                                              description: key is the key to project.
                                              type: string
                                            mode:
                                              description: |-
                                                mode is Optional: mode bits used to set permissions on this file.
                                                Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                                YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                                If not specified, the volume defaultMode will be used.
                                                This might be in conflict with other options that affect the file
                                                mode, like fsGroup, and the result can be other mode bits set.
                                              format: int32
                                              type: integer
                                            path:
                                              description: |-
                                                path is the relative path of the file to map the key to.
                                                May not be an absolute path.
                                                May not contain the path element '..'.
                                                May not start with the string '..'.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: optional specify whether the
                                          ConfigMap or its keys must be defined
                                        type: boolean
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  downwardAPI:
                                    description: downwardAPI information about the
                                      downwardAPI data to project
                                    properties:
                                      items:
                                        description: Items is a list of DownwardAPIVolume
                                          file
                                        items:
                                          description: DownwardAPIVolumeFile represents
                                            information to create the file containing
                                            the pod field
                                          properties:
                                            fieldRef:
                                              description: 'Required: Selects a field
                                                of the pod: only annotations, labels,
                                                name, namespace and uid are supported.'
                                              properties:
                                                apiVersion:
                                                  description: Version of the schema
                                                    the FieldPath is written in terms
                                                    of, defaults to "v1".
                                                  type: string
                                                fieldPath:
                                                  description: Path of the field to
                                                    select in the specified API version.
                                                  type: string
                                              required:
                                              - fieldPath
                                              type: object
                                              x-kubernetes-map-type: atomic
                                            mode:
                                              description: |-
                                                Optional: mode bits used to set permissions on this file, must be an octal value
                                                between 0000 and 0777 or a decimal value between 0 and 511.
                                                YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                                If not specified, the volume defaultMode will be used.
                                                This might be in conflict with other options that affect the file
                                                mode, like fsGroup, and the result can be other mode bits set.
                                              format: int32
                                              type: integer
                                            path:
                                              description: 'Required: Path is  the
                                                relative path name of the file to
                                                be created. Must not be absolute or
                                                contain the ''..'' path. Must be utf-8
                                                encoded. The first item of the relative
                                                path must not start with ''..'''
                                              type: string
                                            resourceFieldRef:
                                              description: |-
                                                Selects a resource of the container: only resources limits and requests
                                                (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.
                                              properties:
                                                containerName:
                                                  description: 'Container name: required
                                                    for volumes, optional for env
                                                    vars'
                                                  type: string
                                                divisor:
                                                  anyOf:
                                                  - type: integer
                                                  - type: string
                                                  description: Specifies the output
                                                    format of the exposed resources,
                                                    defaults to "1"
                                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                                  x-kubernetes-int-or-string: true
                                                resource:
                                                  description: 'Required: resource
                                                    to select'
                                                  type: string
                                              required:
                                              - resource
                                              type: object
                                              x-kubernetes-map-type: atomic
                                          required:
                                          - path
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    type: object
                                  podCertificate:
                                    description: |-
                                      Projects an auto-rotating credential bundle (private key and certificate
                                      chain) that the pod can use either as a TLS client or server.

                                      Kubelet generates a private key and uses it to send a
                                      PodCertificateRequest to the named signer.  Once the signer approves the
                                      request and issues a certificate chain, Kubelet writes the key and
                                      certificate chain to the pod filesystem.  The pod does not start until
                                      certificates have been issued for each podCertificate projected volume
                                      source in its spec.

                                      Kubelet will begin trying to rotate the certificate at the time indicated
                                      by the signer using the PodCertificateRequest.Status.BeginRefreshAt
                                      timestamp.

                                      Kubelet can write a single file, indicated by the credentialBundlePath
                                      field, or separate files, indicated by the keyPath and
                                      certificateChainPath fields.

                                      The credential bundle is a single file in PEM format.  The first PEM
                                      entry is the private key (in PKCS#8 format), and the remaining PEM
                                      entries are the certificate chain issued by the signer (typically,
                                      signers will return their certificate chain in leaf-to-root order).

                                      Prefer using the credential bundle format, since your application code
                                      can read it atomically.  If you use keyPath and certificateChainPath,
                                      your application must make two separate file reads. If these coincide
                                      with a certificate rotation, it is possible that the private key and leaf
                                      certificate you read may not correspond to each other.  Your application
                                      will need to check for this condition, and re-read until they are
                                      consistent.

                                      The named signer controls chooses the format of the certificate it
                                      issues; consult the signer implementation's documentation to learn how to
                                      use the certificates it issues.
                                    properties:
                                      certificateChainPath:
                                        description: |-
                                          Write the certificate chain at this path in the projected volume.

                                          Most applications should use credentialBundlePath.  When using keyPath
                                          and certificateChainPath, your application needs to check that the key
                                          and leaf certificate are consistent, because it is possible to read the
                                          files mid-rotation.
                                        type: string
                                      credentialBundlePath:
                                        description: |-
                                          Write the credential bundle at this path in the projected volume.

                                          The credential bundle is a single file that contains multiple PEM blocks.
                                          The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private
                                          key.

                                          The remaining blocks are CERTIFICATE blocks, containing the issued
                                          certificate chain from the signer (leaf and any intermediates).

                                          Using credentialBundlePath lets your Pod's application code make a single
                                          atomic read that retrieves a consistent key and certificate chain.  If you
                                          project them to separate files, your application code will need to
                                          additionally check that the leaf certificate was issued to the key.
                                        type: string
                                      keyPath:
                                        description: |-
                                          Write the key at this path in the projected volume.

                                          Most applications should use credentialBundlePath.  When using keyPath
                                          and certificateChainPath, your application needs to check that the key
                                          and leaf certificate are consistent, because it is possible to read the
                                          files mid-rotation.
                                        type: string
                                      keyType:
                                        description: |-
                                          The type of keypair Kubelet will generate for the pod.

                                          Valid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384",
                                          "ECDSAP521", and "ED25519".
                                        type: string
                                      maxExpirationSeconds:
                                        description: |-
                                          maxExpirationSeconds is the maximum lifetime permitted for the
                                          certificate.

                                          Kubelet copies this value verbatim into the PodCertificateRequests it
                                          generates for this projection.

                                          If omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver
                                          will reject values shorter than 3600 (1 hour).  The maximum allowable
                                          value is 7862400 (91 days).

                                          The signer implementation is then free to issue a certificate with any
                                          lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600
                                          seconds (1 hour).  This constraint is enforced by kube-apiserver.
                                          `kubernetes.io` signers will never issue certificates with a lifetime
                                          longer than 24 hours.
                                        format: int32
                                        type: integer
                                      signerName:
                                        description: Kubelet's generated CSRs will
                                          be addressed to this signer.
                                        type: string
                                      userAnnotations:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          userAnnotations allow pod authors to pass additional information to
                                          the signer implementation.  Kubernetes does not restrict or validate this
                                          metadata in any way.

                                          These values are copied verbatim into the `spec.unverifiedUserAnnotations` field of
                                          the PodCertificateRequest objects that Kubelet creates.

                                          Entries are subject to the same validation as object metadata annotations,
                                          with the addition that all keys must be domain-prefixed. No restrictions
                                          are placed on values, except an overall size limitation on the entire field.

                                          Signers should document the keys and values they support. Signers should
                                          deny requests that contain keys they do not recognize.
                                        type: object
                                    required:
                                    - keyType
                                    - signerName
                                    type: object
                                  secret:
                                    description: secret information about the secret
                                      data to project
                                    properties:
                                      items:
                                        description: |-
                                          items if unspecified, each key-value pair in the Data field of the referenced
                                          Secret will be projected into the volume as a file whose name is the
                                          key and content is the value. If specified, the listed keys will be
                                          projected into the specified paths, and unlisted keys will not be
                                          present. If a key is specified which is not present in the Secret,
                                          the volume setup will error unless it is marked optional. Paths must be
                                          relative and may not contain the '..' path or start with '..'.
                                        items:
                                          description: Maps a string key to a path
                                            within a volume.
                                          properties:
                                            key:
                                              description: key is the key to project.
                                              type: string
                                            mode:
                                              description: |-
                                                mode is Optional: mode bits used to set permissions on this file.
                                                Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                                YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                                If not specified, the volume defaultMode will be used.
                                                This might be in conflict with other options that affect the file
                                                mode, like fsGroup, and the result can be other mode bits set.
                                              format: int32
                                              type: integer
                                            path:
                                              description: |-
                                                path is the relative path of the file to map the key to.
                                                May not be an absolute path.
                                                May not contain the path element '..'.
                                                May not start with the string '..'.
                                              type: string
                                          required:
                                          - key
                                          - path
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: optional field specify whether
                                          the Secret or its key must be defined
                                        type: boolean
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  serviceAccountToken:
                                    description: serviceAccountToken is information
                                      about the serviceAccountToken data to project
                                    properties:
                                      audience:
                                        description: |-
                                          audience is the intended audience of the token. A recipient of a token
                                          must identify itself with an identifier specified in the audience of the
                                          token, and otherwise should reject the token. The audience defaults to the
                                          identifier of the apiserver.
                                        type: string
                                      expirationSeconds:
                                        description: |-
                                          expirationSeconds is the requested duration of validity of the service
                                          account token. As the token approaches expiration, the kubelet volume
                                          plugin will proactively rotate the service account token. The kubelet will
                                          start trying to rotate the token if the token is older than 80 percent of
                                          its time to live or if the token is older than 24 hours.Defaults to 1 hour
                                          and must be at least 10 minutes.
                                        format: int64
                                        type: integer
                                      path:
                                        description: |-
                                          path is the path relative to the mount point of the file to project the
                                          token into.
                                        type: string
                                    required:
                                    - path
                                    type: object
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                        quobyte:
                          description: |-
                            quobyte represents a Quobyte mount on the host that shares a pod's lifetime.
                            Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.
                          properties:
                            group:
                              description: |-
                                group to map volume access to
                                Default is no group
                              type: string
                            readOnly:
                              description: |-
                                readOnly here will force the Quobyte volume to be mounted with read-only permissions.
                                Defaults to false.
                              type: boolean
                            registry:
                              description: |-
                                registry represents a single or multiple Quobyte Registry services
                                specified as a string as host:port pair (multiple entries are separated with commas)
                                which acts as the central registry for volumes
                              type: string
                            tenant:
                              description: |-
                                tenant owning the given Quobyte volume in the Backend
                                Used with dynamically provisioned Quobyte volumes, value is set by the plugin
                              type: string
                            user:
                              description: |-
                                user to map volume access to
                                Defaults to serivceaccount user
                              type: string
                            volume:
                              description: volume is a string that references an already
                                created Quobyte volume by name.
                              type: string
                          required:
                          - registry
                          - volume
                          type: object
                        rbd:
                          description: |-
                            rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.
                            Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported.
                          properties:
                            fsType:
                              description: |-
                                fsType is the filesystem type of the volume that you want to mount.
                                Tip: Ensure that the filesystem type is supported by the host operating system.
                                Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd
                              type: string
                            image:
                              description: |-
                                image is the rados image name.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              type: string
                            keyring:
                              default: /etc/ceph/keyring
                              description: |-
                                keyring is the path to key ring for RBDUser.
                                Default is /etc/ceph/keyring.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              type: string
                            monitors:
                              description: |-
                                monitors is a collection of Ceph monitors.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                            pool:
                              default: rbd
                              description: |-
                                pool is the rados pool name.
                                Default is rbd.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              type: string
                            readOnly:
                              description: |-
                                readOnly here will force the ReadOnly setting in VolumeMounts.
                                Defaults to false.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              type: boolean
                            secretRef:
                              description: |-
                                secretRef is name of the authentication secret for RBDUser. If provided
                                overrides keyring.
                                Default is nil.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            user:
                              default: admin
                              description: |-
                                user is the rados user name.
                                Default is admin.
                                More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                              type: string
                          required:
                          - image
                          - monitors
                          type: object
                        scaleIO:
                          description: |-
                            scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.
                            Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.
                          properties:
                            fsType:
                              default: xfs
                              description: |-
                                fsType is the filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs".
                                Default is "xfs".
                              type: string
                            gateway:
                              description: gateway is the host address of the ScaleIO
                                API Gateway.
                              type: string
                            protectionDomain:
                              description: protectionDomain is the name of the ScaleIO
                                Protection Domain for the configured storage.
                              type: string
                            readOnly:
                              description: |-
                                readOnly Defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                            secretRef:
                              description: |-
                                secretRef references to the secret for ScaleIO user and other
                                sensitive information. If this is not provided, Login operation will fail.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            sslEnabled:
                              description: sslEnabled Flag enable/disable SSL communication
                                with Gateway, default false
                              type: boolean
                            storageMode:
                              default: ThinProvisioned
                              description: |-
                                storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.
                                Default is ThinProvisioned.
                              type: string
                            storagePool:
                              description: storagePool is the ScaleIO Storage Pool
                                associated with the protection domain.
                              type: string
                            system:
                              description: system is the name of the storage system
                                as configured in ScaleIO.
                              type: string
                            volumeName:
                              description: |-
                                volumeName is the name of a volume already created in the ScaleIO system
                                that is associated with this volume source.
                              type: string
                          required:
                          - gateway
                          - secretRef
                          - system
                          type: object
                        secret:
                          description: |-
                            secret represents a secret that should populate this volume.
                            More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
                          properties:
                            defaultMode:
                              description: |-
                                defaultMode is Optional: mode bits used to set permissions on created files by default.
                                Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                YAML accepts both octal and decimal values, JSON requires decimal values
                                for mode bits. Defaults to 0644.
                                Directories within the path are not affected by this setting.
                                This might be in conflict with other options that affect the file
                                mode, like fsGroup, and the result can be other mode bits set.
                              format: int32
                              type: integer
                            items:
                              description: |-
                                items If unspecified, each key-value pair in the Data field of the referenced
                                Secret will be projected into the volume as a file whose name is the
                                key and content is the value. If specified, the listed keys will be
                                projected into the specified paths, and unlisted keys will not be
                                present. If a key is specified which is not present in the Secret,
                                the volume setup will error unless it is marked optional. Paths must be
                                relative and may not contain the '..' path or start with '..'.
                              items:
                                description: Maps a string key to a path within a
                                  volume.
                                properties:
                                  key:
                                    description: key is the key to project.
                                    type: string
                                  mode:
                                    description: |-
                                      mode is Optional: mode bits used to set permissions on this file.
                                      Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                      YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                      If not specified, the volume defaultMode will be used.
                                      This might be in conflict with other options that affect the file
                                      mode, like fsGroup, and the result can be other mode bits set.
                                    format: int32
                                    type: integer
                                  path:
                                    description: |-
                                      path is the relative path of the file to map the key to.
                                      May not be an absolute path.
                                      May not contain the path element '..'.
                                      May not start with the string '..'.
                                    type: string
                                required:
                                - key
                                - path
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            optional:
                              description: optional field specify whether the Secret
                                or its keys must be defined
                              type: boolean
                            secretName:
                              description: |-
                                secretName is the name of the secret in the pod's namespace to use.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
                              type: string
                          type: object
                        storageos:
                          description: |-
                            storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.
                            Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.
                          properties:
                            fsType:
                              description: |-
                                fsType is the filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                              type: string
                            readOnly:
                              description: |-
                                readOnly defaults to false (read/write). ReadOnly here will force
                                the ReadOnly setting in VolumeMounts.
                              type: boolean
                            secretRef:
                              description: |-
                                secretRef specifies the secret to use for obtaining the StorageOS API
                                credentials.  If not specified, default values will be attempted.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            volumeName:
                              description: |-
                                volumeName is the human-readable name of the StorageOS volume.  Volume
                                names are only unique within a namespace.
                              type: string
                            volumeNamespace:
                              description: |-
                                volumeNamespace specifies the scope of the volume within StorageOS.  If no
                                namespace is specified then the Pod's namespace will be used.  This allows the
                                Kubernetes name scoping to be mirrored within StorageOS for tighter integration.
                                Set VolumeName to any name to override the default behaviour.
                                Set to "default" if you are not using namespaces within StorageOS.
                                Namespaces that do not pre-exist within StorageOS will be created.
                              type: string
                          type: object
                        vsphereVolume:
                          description: |-
                            vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.
                            Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type
                            are redirected to the csi.vsphere.vmware.com CSI driver.
                          properties:
                            fsType:
                              description: |-
                                fsType is filesystem type to mount.
                                Must be a filesystem type supported by the host operating system.
                                Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                              type: string
                            storagePolicyID:
                              description: storagePolicyID is the storage Policy Based
                                Management (SPBM) profile ID associated with the StoragePolicyName.
                              type: string
                            storagePolicyName:
                              description: storagePolicyName is the storage Policy
                                Based Management (SPBM) profile name.
                              type: string
                            volumePath:
                              description: volumePath is the path that identifies
                                vSphere volume vmdk
                              type: string
                          required:
                          - volumePath
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                type: object
              serviceGroup:
                default: default
                description: |-
                  serviceGroup references a service group defined in the parent Workspace's spec.services[].name.
                  The controller resolves the session-api and memory-api endpoints from that group.
                  Defaults to "default".
                maxLength: 63
                pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                type: string
              toolRegistryRef:
                description: toolRegistryRef optionally references a ToolRegistry
                  for available tools.
                properties:
                  name:
                    description: name is the name of the ToolRegistry resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      namespace is the namespace of the ToolRegistry resource.
                      If not specified, the same namespace as the AgentRuntime is used.

                      Cross-namespace references are NOT supported: it must either be omitted
                      or equal the AgentRuntime's own namespace. The agent pod's Role is
                      namespace-scoped, so a foreign namespace is unreadable from the pod and
                      registry-scoped ToolPolicies would not match. An AgentRuntime declaring
                      one is rejected with ToolRegistryCrossNamespace.
                    type: string
                required:
                - name
                type: object
            required:
            - facades
            - promptPackRef
            type: object
            x-kubernetes-validations:
            - message: spec.inputSchema is required when spec.mode is 'function'
              rule: self.mode != 'function' || has(self.inputSchema)
            - message: spec.outputSchema is required when spec.mode is 'function'
              rule: self.mode != 'function' || has(self.outputSchema)
            - message: spec.inputSchema is only valid when spec.mode is 'function'
              rule: self.mode == 'function' || !has(self.inputSchema)
            - message: spec.outputSchema is only valid when spec.mode is 'function'
              rule: self.mode == 'function' || !has(self.outputSchema)
            - message: spec.outputFormat is only valid when spec.mode is 'function'
              rule: self.mode == 'function' || !has(self.outputFormat)
            - message: spec.facades must not contain duplicate facade types
              rule: '!has(self.facades) || self.facades.all(f, self.facades.exists_one(g,
                g.type == f.type))'
            - message: mode 'agent' allows only 'websocket', 'a2a' and 'custom' facades
              rule: '!has(self.facades) || self.mode != ''agent'' || self.facades.all(f,
                f.type == ''websocket'' || f.type == ''a2a'' || f.type == ''custom'')'
            - message: mode 'function' allows only 'rest' and 'mcp' facades
              rule: '!has(self.facades) || self.mode != ''function'' || self.facades.all(f,
                f.type == ''rest'' || f.type == ''mcp'')'
            - message: mode 'function' requires exactly one 'rest' facade
              rule: '!has(self.facades) || self.mode != ''function'' || self.facades.exists_one(f,
                f.type == ''rest'')'
            - message: facade type 'custom' requires spec.facades[].image
              rule: '!has(self.facades) || self.facades.all(f, f.type != ''custom''
                || (has(f.image) && size(f.image) > 0))'
            - message: spec.rollout.trigger requires a version-pinned spec.promptPackRef
                and is mutually exclusive with promptPackRef.track
              rule: '!(has(self.rollout) && has(self.rollout.trigger)) || (has(self.promptPackRef)
                && has(self.promptPackRef.version) && !has(self.promptPackRef.track))'
          status:
            description: status defines the observed state of AgentRuntime
            properties:
              a2a:
                description: a2a holds A2A-specific status information when an a2a
                  facade is present.
                properties:
                  agentCardURL:
                    description: agentCardURL is the URL where the agent card is served.
                    type: string
                  clients:
                    description: clients reports the resolution status of each configured
                      A2A client.
                    items:
                      description: A2AClientStatus reports the resolution state of
                        an A2A client connection.
                      properties:
                        error:
                          description: error contains the resolution error message,
                            if any.
                          type: string
                        name:
                          description: name matches the client name from the spec.
                          type: string
                        ready:
                          description: ready indicates whether the client was successfully
                            resolved.
                          type: boolean
                        resolvedURL:
                          description: resolvedURL is the resolved A2A endpoint URL.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  endpoint:
                    description: endpoint is the A2A JSON-RPC endpoint URL.
                    type: string
                type: object
              activeVersion:
                description: activeVersion is the currently deployed PromptPack version.
                type: string
              conditions:
                description: conditions represent the current state of the AgentRuntime
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              facade:
                description: |-
                  facade reports externally-reachable endpoints derived from observed
                  Gateway API HTTPRoutes. Empty => the agent is reachable only in-cluster.
                properties:
                  endpoints:
                    description: |-
                      endpoints are the external URLs derived from HTTPRoutes that target this
                      agent's facade Service. Empty => cluster-internal only.
                    items:
                      description: |-
                        FacadeEndpoint is one externally-reachable URL for the agent's facade,
                        derived from an observed HTTPRoute. Auth is NOT included here; it is read
                        from spec.externalAuth (agent-global) by consumers.
                      properties:
                        host:
                          description: host is the route hostname.
                          type: string
                        path:
                          description: path is the external path including the protocol's
                            canonical suffix.
                          type: string
                        port:
                          description: port is the Service backend port the route
                            targets.
                          format: int32
                          type: integer
                        protocol:
                          description: protocol is the facade protocol this endpoint
                            serves.
                          enum:
                          - websocket
                          - a2a
                          - mcp
                          - rest
                          type: string
                        reason:
                          description: reason explains why valid is false.
                          type: string
                        routeName:
                          description: routeName is the name of the HTTPRoute this
                            endpoint was derived from.
                          type: string
                        routeNamespace:
                          description: routeNamespace is the namespace of that HTTPRoute.
                          type: string
                        scheme:
                          description: 'scheme is the URL scheme: ws, wss, http, or
                            https.'
                          type: string
                        url:
                          description: url is the client-facing connection URL, e.g.
                            wss://agents.example.com/my-agent/ws
                          type: string
                        valid:
                          description: |-
                            valid is false when the endpoint is advertised but will not actually
                            connect (e.g. a path prefix that is not stripped before the facade).
                          type: boolean
                      required:
                      - host
                      - path
                      - port
                      - protocol
                      - routeName
                      - routeNamespace
                      - scheme
                      - url
                      - valid
                      type: object
                    type: array
                type: object
              managementEndpoints:
                description: |-
                  managementEndpoints reports the internal (management-plane) listener ports
                  the facades serve, one entry per surface whose facade has
                  managementPlane enabled (the default). A surface's field is nil when no
                  such facade exists or its managementPlane is false. The dashboard and
                  in-cluster callers read these to dial the agent over the management plane
                  — they never compute the port from the external port.
                properties:
                  a2a:
                    description: a2a is the internal A2A management-plane port (dual-protocol
                      agents).
                    format: int32
                    type: integer
                  mcp:
                    description: mcp is the internal MCP management-plane port (function-mode
                      agents).
                    format: int32
                    type: integer
                  ws:
                    description: ws is the internal WebSocket management-plane port.
                    format: int32
                    type: integer
                type: object
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase represents the current lifecycle phase of the AgentRuntime.
                enum:
                - Pending
                - Running
                - Failed
                type: string
              replicas:
                description: replicas tracks the replica counts for the deployment.
                properties:
                  available:
                    description: available is the number of available replicas.
                    format: int32
                    type: integer
                  desired:
                    description: desired is the desired number of replicas.
                    format: int32
                    type: integer
                  ready:
                    description: ready is the number of ready replicas.
                    format: int32
                    type: integer
                required:
                - available
                - desired
                - ready
                type: object
              rollout:
                description: rollout reports the current state of an active rollout,
                  if any.
                properties:
                  active:
                    description: active indicates whether a rollout is currently in
                      progress.
                    type: boolean
                  candidateVersion:
                    description: candidateVersion is the version identifier of the
                      candidate being rolled out.
                    type: string
                  currentStep:
                    description: currentStep is the zero-based index of the step currently
                      being executed.
                    format: int32
                    type: integer
                  currentWeight:
                    description: currentWeight is the percentage of traffic currently
                      sent to the candidate.
                    format: int32
                    type: integer
                  message:
                    description: message is a human-readable description of the current
                      rollout state.
                    type: string
                  promoting:
                    description: |-
                      promoting indicates a promotion is in progress: spec has advanced to the
                      candidate config and the stable Deployment is rolling to it in the
                      background, while the validated candidate keeps serving 100% of traffic.
                      Cleared once the stable Deployment is healthy on the new config and
                      traffic has cut back to it. This is what makes promotion zero-downtime —
                      no request is served from a cold/restarting stable pod.
                    type: boolean
                  stableVersion:
                    description: stableVersion is the version identifier of the current
                      stable deployment.
                    type: string
                  startedAt:
                    description: startedAt is the RFC3339 timestamp when the rollout
                      began.
                    type: string
                  stepStartedAt:
                    description: |-
                      stepStartedAt is the RFC3339 timestamp when the controller entered
                      the step pointed to by currentStep. Used to honour pause durations
                      across reconciles: a pause step only advances once
                      (now - stepStartedAt) >= the pause duration.
                    type: string
                  trafficRoutingMode:
                    description: |-
                      trafficRoutingMode is the mode the controller actually resolved to (may
                      differ from spec.trafficRouting.mode when degraded).
                    type: string
                  trafficWeightEnforced:
                    description: |-
                      trafficWeightEnforced is true when delivered weight matches the reported
                      step (mode mesh/external), false when approximated by replica ratio
                      (mode replicaWeighted). Lets dashboards label the weight "approx".
                    type: boolean
                type: object
              runtimeCapabilities:
                description: |-
                  runtimeCapabilities are the contract features the running runtime
                  advertises, self-reported at startup. An open set — the control plane
                  displays whatever is reported. Empty means the runtime predates capability
                  advertisement (legacy) or has not reported yet.
                items:
                  type: string
                type: array
              serviceEndpoint:
                description: |-
                  serviceEndpoint is the internal Kubernetes service endpoint for the agent facade.
                  Format: {name}.{namespace}.svc.cluster.local:{port}
                  This can be used by dashboard or other services to connect to the agent.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
