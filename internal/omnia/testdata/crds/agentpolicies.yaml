---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: agentpolicies.omnia.altairalabs.ai
spec:
  group: omnia.altairalabs.ai
  names:
    kind: AgentPolicy
    listKind: AgentPolicyList
    plural: agentpolicies
    singular: agentpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.matchedAgents
      name: Matched
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AgentPolicy defines claim-to-header mapping rules for agent communication.
          It configures how JWT claims are extracted and propagated as headers through
          the facade -> runtime -> tool adapter pipeline.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AgentPolicy
            properties:
              mode:
                default: enforce
                description: |-
                  mode is the enforcement mode: "enforce" (default) or "permissive".
                  In permissive mode, policy decisions are logged but traffic is not blocked.
                enum:
                - enforce
                - permissive
                type: string
              onFailure:
                default: deny
                description: 'onFailure defines behavior when policy evaluation fails:
                  "deny" (default) or "allow".'
                enum:
                - deny
                - allow
                type: string
              selector:
                description: selector determines which agents this policy applies
                  to.
                properties:
                  agents:
                    description: |-
                      agents is a list of AgentRuntime names this policy applies to.
                      If empty, the policy applies to all agents in the namespace.
                    items:
                      type: string
                    type: array
                type: object
              toolAccess:
                description: toolAccess defines tool allowlist/denylist rules.
                properties:
                  mode:
                    description: 'mode is the access control mode: "allowlist" or
                      "denylist".'
                    enum:
                    - allowlist
                    - denylist
                    type: string
                  rules:
                    description: rules is the list of tool access rules.
                    items:
                      description: ToolAccessRule identifies tools within a specific
                        registry.
                      properties:
                        registry:
                          description: registry is the name of the ToolRegistry resource.
                          minLength: 1
                          type: string
                        tools:
                          description: tools is the list of tool names within the
                            registry.
                          items:
                            type: string
                          minItems: 1
                          type: array
                      required:
                      - registry
                      - tools
                      type: object
                    minItems: 1
                    type: array
                required:
                - mode
                - rules
                type: object
            type: object
          status:
            description: status defines the observed state of AgentPolicy
            properties:
              conditions:
                description: conditions represent the current state of the AgentPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              matchedAgents:
                description: matchedAgents is the count of agents matched by the selector.
                format: int32
                type: integer
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase represents the current lifecycle phase of the AgentPolicy.
                enum:
                - Active
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
